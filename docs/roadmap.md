# hdClaude roadmap and phase tracker

Status: live. Last revised 2026-09-21.

This is the durable progress record. Update the tracker, the evidence column,
and the decision log **in the same commit** as the work they describe. A phase
is not complete because its API exists; it is complete when its exit gate passes
and the evidence is recorded here.

## How to resume

1. Read the decision log and the phase tracker below.
2. Inspect the worktree and recent commits. **Never assume this document is
   newer than the code.**
3. Run `compile.bat` and the full test suite before changing renderer
   behaviour, and `render_gallery.bat` after.
4. Work one reviewable phase boundary at a time.
5. Re-render and commit the full gallery after every render-affecting change.
6. Record timing, image, validation, and hardware evidence in the phase entry.

## Phase tracker

Allowed states: `Not started`, `In progress`, `Blocked`, `Complete`, `Deferred`.
A blocked phase must name what blocks it.

| # | Phase | State | Evidence |
| --- | --- | --- | --- |
| 0 | Repository, build system, environment, documentation | Complete | core-only preset builds and tests from clean; env script verified; 10 gallery stages open |
| 1 | Core library: hashing, shader cache, spectral tables, display transform | Complete | hashing, shader cache, spectral sampling and sensor; the shader cache misses on every component of its key. RGB-to-spectrum upsampling: Jakob-Hanika sigmoid for reflectance, whose range is (0, 1) for any coefficients at all so no fit can produce an albedo that creates energy, and a bounded chromaticity plus a carried magnitude for emission, which is unbounded. The round-trip gate is met -- upsample and integrate back within 1e-3 dE2000 -- at 4.7e-5 worst case over the primaries, secondaries, a grey ramp and a spread of muted reflectances, with CIEDE2000 itself checked against published pairs. The integral is adapted so a perfect reflector produces the colour space's white: the CMF fit and the illuminant table are each approximations whose product lands a fraction of a per cent off D65, which every colour but white can absorb into its fit. 3232 checks pass. The gate's Munsell half is now asserted as written rather than stood in for by a set chosen by eye: the 24 ColorChecker patches, each defined by a Munsell notation, from X-Rite's published D65 sRGB values, round-trip at a worst of 9.4e-5 dE2000 (neutral 5, N 5/), and the saturated patches near the sRGB boundary -- orange, yellow, cyan -- stay inside [0, 1] at every wavelength. The suite builds and passes under `core-only`, whose test binary depends on the C runtime alone: no OpenUSD and no Vulkan. 5864 checks pass |
| 2 | Vulkan context, memory, resource rules, validation gate | Complete | context, allocator, buffers, images, generations, device-lost latch, compute pipelines and direct and indirect dispatch; core *and* synchronisation validation clean on RTX 5060 Ti. Acceleration-structure scratch addresses are aligned to `minAccelerationStructureScratchOffsetAlignment`, which is a build requirement no allocator enforces and whose violation is a lost device. The exit gate is now asserted in full rather than in part. Four suites -- gpu, geometry, closure validation and render -- run with the layer and fail on a nonzero error count, and they now also refuse to run when synchronisation validation is off, which it can be with the layer present (it is requested through `VK_EXT_layer_settings`); before, a layer without that extension gave a clean count while checking only core validation. Device loss: the latch sets, `RequireLive` and `SubmitImmediate` refuse, and teardown of a lost context issues no `vkDeviceWaitIdle` -- counted process-wide through `DeviceWaitsIssuedForTesting`, since the wait that matters is a destructor's and a simulated loss leaves a healthy device underneath, so an unwanted wait would succeed rather than crash. A control asserts a live teardown issues exactly one, and forcing the lost path to wait fails the suite at that check |
| 3 | Geometry pipeline: meshes, BLAS/TLAS, instancing, subdivision | In progress | prototypes, fingerprinted BLAS reuse, TLAS instancing, ray-query traversal verified by hit pattern on GPU. per-triangle materials from GeomSubsets; deformation through ExtComputation; uniform OpenSubdiv refinement carrying subsets through the parent-face chain; an instance transform shades identically to the same tilt baked into the prototype, which is what catches a basis used where its transpose belongs; refinement carries texture coordinates through the same weights as the positions, and structure reuse is keyed on the normals, UVs and per-triangle materials the structure owns as well as on the geometry it is built from. Authored normals are read from the primvar descriptors in whatever interpolation they were authored in -- which is what finds both spellings USD allows, `normals` and `primvars:normals`, and what stops a face-varying or uniform set being dropped for not being one per vertex; a crease can only be authored face-varying, since its two sides need different normals at the same vertex, and 195 of the OpenPBR Playground's meshes author exactly that. Subdivision tags -- creases, corners, the boundary rule and the face-varying interpolation rule -- are read from the scene delegate and set on the topology, which `GetMeshTopology` does not carry: without that call the refiner silently used OpenSubdiv's defaults, which disagree with USD's on every one of them, so a creased edge refined smooth and an open boundary floated inward instead of being pinned. The adapter path now has a test that would have caught the Kitchen Set defect: `usd_instancing` (`tests/usd/instancing_equivalence.py`) renders a model of four offset, turned and scaled meshes placed through native instances, a PointInstancer, and a PointInstancer inside a native instance, and requires it to match the same geometry flattened to world space by USD's own `UsdGeomXformCache` and `ComputeInstanceTransformsAtTime` (RMS 0.00097 at 64 samples); a third stage that places each mesh by its instance alone, as the defect did, must fail the same comparison, and does (RMS 0.116, 15% of pixels). **Refinement is adaptive on request**, which is the first half of what remained. `Adaptive subdivision` gives each mesh the level at which its *refined* edges are about `Subdivision edge pixels` long, from the pinhole projection and nothing more: the frame spans `2 * distance * tanHalfFov` world units, so a coarse edge covers a known number of pixels and the level that brings it to the target is the log base two of the ratio, rounded up. Ten times the distance is three levels coarser, which is checked as arithmetic rather than as a picture -- in `hdClaudeCore`, on any host, with no GPU and no stage. On Pixar's Kitchen Set it renders 2,109,620 triangles where uniform level 2 renders 8,706,088, in 812 MiB of device memory against 1.29 GiB, and the two images are indistinguishable at 1024 px: rms 0.0085 against the gate's 0.01, over 2.6% of the frame. Tracing is *not* faster -- 106.7 s against 104.7 s -- which is the honest result and worth stating: a BVH is logarithmic in its primitive count and this frame's cost is shading and rays, so what adaptive refinement buys here is memory and build time, not traversal. Three things about how it is asked. The view is **sampled, not followed**: it is taken when there is none, when a refinement setting changes, and when `Retessellate` is bumped, and a camera move is not a reason unless `Subdivision follows camera` is turned on. Off-screen geometry is held at a **floor rather than culled**, because a path tracer sees what the camera does not -- in a mirror, through glass, as a shadow, in every indirect bounce -- and that insurance costs 4% of the triangles on the Kitchen Set (2,109,620 at floor 1 against 2,023,846 at floor 0). And the limit is now a **face budget** rather than a cap on the level: the level may reach 10 where it used to stop at 6, and what refuses is `Subdivision face budget`, which names the mesh it applied to and what that mesh wanted. Off by default, so every committed image is unchanged and six of the eleven gallery scenes are bit-identical. One defect found on the way, and it was not in this work: hdClaude's existing resync for the curve and subdivision settings called `HdChangeTracker::MarkRprimDirty`, which a render index driven by scene indices refuses outright -- and that is the pipeline `usdrecord` and `usdview` have used since OpenUSD turned scene indices on by default. It had therefore never fired in either of them, silently, because a resync that does not happen looks exactly like a setting nobody changed. hdClaude now inserts a pass-through scene index of its own and sends the notice from there. 5924 checks pass in the core suite where 5864 did, and all eleven suites pass. Remaining: per-face adaptive refinement, so a single large mesh -- the height map, a terrain, a floor -- can be fine where it is near and coarse where it is far, which per-mesh refinement cannot do; and limit-surface normals |
| 4 | `genglsl_pt` MaterialX target: eval, sample, pdf, combinators | Complete | all 22 overrides; `standard_surface` and `open_pbr_surface` compile; energy and probability-mass validated on GPU for 10 closures and combinators, and a dielectric's reflectance validated from *inside* as well -- 0.0859 against a closed form's 0.0860 at 35 degrees of internal incidence and 0.9980 against 1.0 past the critical angle, which is the side every earlier measurement was blind to because the two Fresnel curves agree near normal and a path could not reach the angles where they do not; the generated geometry setter assigns every member it declares, so object-space patterns and texture coordinates reach a material instead of reading undefined memory; screen-space derivatives are defined as zero, which is a path tracer's actual filter width, so an inherited node that asks for one generates instead of failing to compile; and the bitangent is taken from the kernel rather than rebuilt as `cross(N, T)`, because its sign belongs to the parameterisation and a mirrored UV island -- how the second half of a symmetric asset is laid out -- inverts every normal-mapped detail without it. Chi-squared agreement now holds for every closure and all four combinators, *nested* as well as alone -- 23 cases, sampled directions against the reported density integrated over each of 800 cells of the sphere, asked the way `shade` asks -- and it found four real defects on its first runs (decision log, 2026-09-14). The three nested cases found a fifth: every combinator and both RT splits chose a lobe from the same `hdclaude_sample_u.z`, and MaterialX emits a node's children before the node, so a nested choice was the same choice as its parent's -- `mix(mix(a, b), c)` never sampled `b` while its density gave `b` a quarter, at p = 0 and 3.6% of the light in the furnace. Each choice now hashes that number with a per-invocation count, and the nested mix reads 0.74742 against its quadrature's 0.74742 (decision log, 2026-09-15). And item 2 as written: the importance-sampled albedo of 16 closures and combinators agrees with the response integrated over the sphere to a worst of 0.053%, with white smooth Oren-Nayar and white translucent at exactly one both ways; which found every anisotropic GGX density using the averaged roughness's masking, 0.87% off at 0.2 by 0.6, now 0.041%. Item 1 holds on the gallery: no material in eleven scenes reports being shaded other than as authored. Item 4 is read with item 5, as the acceptance section itself reads it: a below-horizon visible-normal sample has zero density and zero weight. All five acceptance items are met (materialx-codegen.md 9) |
| 5 | Wavefront integrator: queues, sort, per-material dispatch | In progress | raygen, extend, environment, per-material shade, shadow, film; compaction between bounces; the counting sort by material and indirect dispatch throughout, asserted against the GPU-written counts (2336 camera hits split 1169/1167 across two materials, matching the shaded pixels exactly); the gate's scaling measurement, at 512x512 with a 32-node fractal graph -- 8.7 ms with neither quad heavy, 18.6 ms with one, 29.1 ms with both, so one heavy material costs 0.45 of what two cost rather than nearly all of it; images verified for direct lighting, per-material dispatch and cast shadows; a sampled direction is evaluated by the closure whose side it left on, so a refraction reaches the transmission branch instead of being terminated as impossible. The tangent frame handed to a material is solved from the triangle's texture coordinates rather than taken from an edge, which is the frame a tangent-space normal map is actually defined against -- asserted by a material whose albedo is its own tangent, on a quad whose `u` runs across its edges -- and per-corner normals are indexed by primitive, so a crease stays a crease. Remaining: sort granularity, once there is a scene that makes the choice matter |
| 6 | Spectral transport, lights, MIS, film | In progress | the analytic UsdLux set -- rect, disk, sphere, cylinder, distant -- sampled by NEE, with cone and focus shaping and colour temperature, and light and shadow linking through Hydra's categories (checked at the USD level against stages the specification says render the same, each with a stage that must not); a dome light supplies a constant or textured environment, in USD's own latitude-longitude orientation; exposure control; film accumulates progressively. The environment is sampled as one more emitter by next-event estimation and weighted against BSDF sampling by the balance heuristic -- the analytic lights deliberately are not, since nothing can hit them -- and a white furnace confirms the estimator: a 0.8 Lambertian under a unit sky renders 0.811 at 256 samples, where the closed form is 0.800. The environment is sampled from its own luminance -- a marginal CDF over the map's rows and a conditional CDF per row, each texel weighted by luminance times `sin(theta)` so a pole's rows do not take samples the solid angle does not justify -- and the density stays a function of direction alone, which is the only kind an MIS weight can be when the kernel that needs it has a miss and no surface. Two furnace tests stand behind it: a uniform dome, whose (u, v) density is proportional to `sin(theta)` and so catches a missing Jacobian, renders 0.818 against 0.800; and a sky lit only for `u < 0.5` -- the half-space x > 0 -- renders 0.402 against the 0.400 the cosine weight's symmetry requires, which is what pins the reconstruction's orientation rather than only its measure. Transport is spectral: a path carries four hero wavelengths drawn at ray generation, throughput and radiance are per lane, and RGB survives only where an asset authors one and where the film hands an image back. A closure's response upsamples as a reflectance and a light's colour as an emission -- the same spectrum times the illuminant its RGB was authored against, since a white light emits D65 -- and the film divides by that illuminant's luminous integral, so a white surface under a white light resolves to white. The colour matching functions, the illuminant and the chromaticity table are sampled on the host and uploaded rather than re-derived in GLSL, because a second closed form is a second thing to keep in agreement with the fit its round-trip gate is written against. A furnace confirms the estimator end to end: a 0.8 grey under a unit sky renders 0.804, 0.801, 0.804. A colour temperature is transported as a spectrum rather than resolved to a tint on the host: a light or dome with `enableColorTemperature` is referred to its own Planck spectrum *in place of* D65 -- a 2700 K light does not emit daylight through an amber filter -- scaled so the blackbody carries the same luminous power as the illuminant it replaces, which is what makes the control change hue and not brightness. The spectrum is pinned against the published Planckian locus at 2000, 3000, 4000, 6500 and 10000 K, within 0.0025 in x and y, which checks Planck's law and the colour matching integration together rather than either alone; and on the GPU a lit quad's red-to-blue ratio moves from 0.998 neutral to 10.011 at 2700 K and 0.673 at 9000 K while its luminance holds at 0.2550 in all three, which is the pair of claims -- it tints, it does not brighten -- that a peak-normalised blackbody would fail. Eight of the ten gallery scenes re-render byte-identical, since none of them enables the control. The analytic lights are emitters a scattered ray can reach: they stay out of the acceleration structure and are intersected in closed form, which is what a rect and a sphere already are, so a light occludes what is behind it and appears in a reflection. Next-event estimation and BSDF sampling therefore both reach them and are weighed by the balance heuristic. The partition is checked against a closed form rather than against a previous render: a 0.8 Lambertian under a rect emitter of radiance 3 at unit half-extents and height 2 renders 0.5716, where the point-to-rectangle configuration factor gives 0.5747 -- a total that double counting overshoots and an unmatched weight undershoots. A light seen directly reads the radiance it was given, 0.4967 against 0.50. A smooth dielectric reflects exactly its Fresnel share: under a uniform sky, viewed head on, it renders 0.0402 at IOR 1.5 against the closed form's 0.0400, 0.0455 at 1.54107 against 0.0453, and 0.1116 at 2.0 against 0.1111. And the MIS split conserves what it divides: under a rect light wide enough to swallow the whole specular lobe, a delta mirror and a 0.02-roughness gloss of the same IOR both render 0.0800 against the closed form's 0.0800, and so do both transmissive cases -- 0.0808 for the delta and 0.0810 for the gloss, which is the shader ball's glass exactly and the one combination where the two densities could have disagreed about whether a lobe's probability is folded into it -- a closure that dropped its reflection lobe once transmission was enabled would look exactly like a glass ball with a bare shell and would pass every reflection-only test -- the assertion that a near-mirror lobe needs, because the balance heuristic gives next-event estimation almost nothing there and a missing scattered-ray half would delete the highlight rather than roughen it, with every furnace still passing. That is the number a glass material is right or wrong by, and it is small enough that a plausible image survives getting it wrong -- glass reflecting a light at a twentieth of gold's strength looks much like glass not reflecting it at all. A scene of lights and no geometry now builds a valid empty top-level structure rather than a null descriptor. A transmissive material's interior is transported: `anisotropic_vdf` publishes an absorption coefficient and the traversal kernel applies Beer-Lambert over the flight it just measured, which is the only kernel that knows the distance. The transmittance is upsampled rather than the coefficient -- `exp(-sigma d)` is bounded in (0, 1] whatever the coefficient is, which is the range the reflectance fit is built for, and it keeps RGB becoming spectral at the closure boundary and nowhere else. Asserted as a *ratio* against the same surface with a clear interior, so whatever constant `open_pbr_surface` puts on transmission cancels and only the medium is under test: 0.6402, 0.3596, 0.1599 against the closed form's 0.6400, 0.3600, 0.1600. A medium also scatters: `extend` walks the path through it, sampling a free flight and a Henyey-Greenstein direction until it reaches a boundary, which turns honey from a clear amber into the cloudy material it is (mean 0.402 to 0.635). The walk lives in the traversal kernel because a scattering event has no material and so no bin in the per-material sort; a path still leaves that kernel with exactly one surface hit. Remaining on the volume: scattering is *achromatic*, because a chromatic coefficient sampled against one control wavelength makes every other lane carry a weight that grows with the flight and compounds over a walk until it overflows -- it produced NaN before being cut back -- and fixing that properly is multiple importance sampling across the lanes' densities. Colour in a medium therefore comes from absorption, which is spectral and exact. There is also no next-event estimation at a scattering vertex, and for the interiors hdClaude has, that is less of a gap than it sounds: a shadow ray from inside can reach only an emitter in the same closed interior, because every boundary is either a refraction or the cosine lobe `subsurface_bsdf` enters and leaves by, and a straight line to a light outside crosses one without the deviation the boundary requires. The estimate the boundary admits is the one `shade` already makes where the walk leaves (decision log, 2026-09-15). The volumetric white furnace that would gate the walk -- a closed slab that absorbs nothing, in a uniform environment of unit radiance, must render exactly one however the light gets around inside it -- now gates a bare `dielectric_bsdf` slab, which renders 1.0001, 0.9987, 0.9979 -- the closure conserves energy exactly. A **sphere** furnace now stands beside it, and it is the first gate here that varies the angle at all: two flat quads present a delta refraction at one incidence per pixel and never a steep one, so every furnace before it was a near-normal measurement and an error that vanishes at normal incidence could not have been caught. The closures hold up -- a bare `dielectric_bsdf` reads 1.0065 and 0.9900 across the disc, `layer(R, T)` reads 1.0003 and 0.9894, and `layer(R, layer_vdf(T, anisotropic_vdf))` enclosing a vacuum reads what the layered dielectric reads, which puts any volume failure inside the volume rather than around it. The walk does too, now. A sphere of a lossless scattering medium read **1.4214** at an interface index of 1.5 and reads 1.0110; forward scattering, a dense medium and a chromatic one at four to one across the channels all land within 1.2 per cent of one and neutral, and the gate is asserted. The defect was not in the walk and not in the layering: `mx_dielectric_bsdf` handed the *absolute* index to both sides of the interface, so a path inside glass saw the reflectance curve of a path outside it -- a factor of two wrong at 35 degrees of internal incidence and a factor of twenty past the critical angle, where the true value is one. Nothing reached those angles before a scattering interior existed, since refraction maps the whole outside hemisphere into a cone of exactly the critical half-angle. Three things moved together: the Fresnel now gets the relative index, which is all `mx_fresnel_dielectric` ever needed to return its own total internal reflection; the directional albedo `layer` reads as an energy split is weighted by the Fresnel actually in force rather than by a Schlick fit that cannot climb to one before grazing; and the reflect/refract split's `[0.05, 0.95]` clamp is gone, since with a correct Fresnel the two conditions it guarded against coincide, and its ceiling was costing five per cent on every crossing past the critical angle. Asking *which side* was the hard part, and two wrong answers were caught by the gallery before the third was right: the shading normal tilts past the horizon near a silhouette, which put a rim of total internal reflection around every rounded opaque object in the subdivision scene, and `scatter_mode` cannot separate the reflection half of a transmissive interface from a coat over an opaque substrate. Only the path's own history answers it, so the shade kernel publishes both the geometric normal and whether the path is inside a medium, and a closure reads the inside curve only when a transmission event put it there. The closure validation suite gained internal incidences to pin it: 0.0859 against a closed form's 0.0860 at 35 degrees, 0.9980 against 1.0 past the critical angle. Two furnaces that already passed improved with it, the layered dielectric slab from 0.9953 to 0.9978. Spectral MIS across the lanes now ships, per *walk* rather than per step. Choosing the proposing lane once per step is unbiased and bounds each step's weight by the lane count, but a walk multiplies steps and the path's weight is then bounded only by the lane count raised to the step count: that form read 1.02, 1.06, 1.01 on a four-to-one medium and did not tighten at eight times the samples, while being exact on a medium thin enough to scatter once. Choosing once per walk applies the balance heuristic to the whole walk's density, which has a closed form -- `sigma_s[j]^collisions * exp(-sigma_s[j] * distance)`, since a product of exponentials is an exponential of a sum -- so the walk carries a collision count and a total distance and no per-step product at all, evaluated in logarithms with the largest term factored out. Roulette moved onto the accumulated absorption, the part of the weight that actually decays, with its compensation carried as a scalar to the end. It converges: the chromatic spread falls from 3.2 per cent at 256 samples to 1.4 at 2048, about the square root of the sample ratio, where the per-step form stayed near five at both. The gate reads 1.0075 isotropic, 1.0035 forward, 1.0067 dense, and 0.9991, 1.0037, 1.0085 chromatic at four to one. Three exact guards keep it attributable -- a path in vacuum draws nothing, scattering is tested on the *authored* coefficient rather than the upsampled one, which is a millionth rather than zero for a clear glass, and an achromatic medium draws nothing since its four techniques are one technique. Seven gallery scenes stay byte identical, Sponza moves by an RMS of 0.0037 and the glass ball and playground by 0.031 and 0.057, and every per-channel mean is unchanged to four decimal places: the movement is resampling, and no asset here has a medium chromatic enough to show the transport as a colour. The same slab built from `open_pbr_surface` read **1.1505** -- fifteen per cent gained per crossing -- and now reads 1.0145. The defect was in `mx_layer_bsdf`, which clamped the probability of selecting its top layer to a floor of 0.05: `open_pbr_surface` layers a coat and a fuzz over its base whether or not they are switched on, and a layer of zero weight has zero albedo, so three dead lobes were each claiming five per cent of a mixture density that no longer described the sampling. Zero is safe because the two conditions coincide -- a top layer with unit throughput has taken nothing, so its response is zero and there is nothing to weigh. Against hdCodex the glass ball falls from 0.145 to 0.113 and the playground from 0.248 to 0.236, which is the independent check. The residual 1.45 per cent was a four per cent loss and a five per cent gain that nearly cancelled, and both are fixed. The loss was `mx_layer_bsdf` applying `1 - F` twice, once as the reflection lobe's throughput and once inside the transmission lobe's own response; a transmission-only lobe exists to be layered under a reflection one, and MaterialX's convention is that a base does not know the top's Fresnel, so it no longer carries it. The gain was `mx_dielectric_bsdf` treating a single-lobe request as though it still had to choose, reflecting one sample in twenty nobody asked for and dividing its density by 0.95; the split now applies only in RT mode, where the closure genuinely has two lobes to weigh. Fixing either alone makes the image worse -- removing the gain alone takes the furnace to 0.9206 -- which is why they had to be found together. A lossless slab now renders 0.9953 against one, the layered surface agreeing with the bare closure's own noise floor, and the gate is tightened to two per cent. That is the same excess the medium's own test had to be written as a ratio to cancel. Energy conservation of a transmissive surface is therefore the next defect to fix, and the furnace goes in with it rather than being committed at a tolerance that would record the defect as correct. And subsurface is still not transported at all -- it is carried to the same medium machinery, but `mx_subsurface_bsdf` only ever evaluates the reflection closure and never samples a direction *into* the surface, so the hand-off is never reached and the bubblegum ball renders byte-identically to before. A conservation gate for it now exists ahead of the transport: a closed slab of `subsurface_bsdf` at unit albedo, whose radius carries the zero component the bubblegum asset authors, renders 0.9995 in a unit furnace. It passes today because subsurface behaves as a Lambertian, and it is there so that it still has to once a walk is carrying the light. **Subsurface is transported.** All three of the pieces this entry used to name as remaining are done, and a fourth defect turned up between them. The colour mapping is OpenPBR's own: `subsurface_bsdf`'s `color` is documented as the observed reflection colour, which is not the fraction of a collision that survives, and van de Hulst's inversion between them -- which OpenPBR states in closed form -- says a medium whose collisions each survive with probability 0.6 returns about 0.19 of what enters it. Handing the authored colour to a walk as its scattering albedo therefore renders every subsurface material about three times too dark, and looks like a lighting problem. The inversion is checked against the forward relation it inverts rather than against remembered values, and round trips to 4.7e-5 across the range at four anisotropies. The extinction is `1 / radius` with the regularization OpenPBR's text asks for in the limit `r -> 0`, which the bubblegum asset's `(1, 0, 0.068)` requires: a floor on the *ratio* between channels rather than an epsilon on the value, at the 18.42 that is the largest ratio the spectral mapping of a coefficient can carry, and invisible because a medium that dense is in the diffusive regime where van de Hulst's reflectance does not depend on the mean free path at all. A radius of zero in every channel is solved rather than regularized: the light leaves where it entered, so the closure is exactly a Lambertian of that reflectance. The publication question had a worse answer than expected. Both `standard_surface` and `open_pbr_surface` instantiate `subsurface_bsdf` unconditionally with `weight` hardwired to one and gate it downstream with a `mix`, and `standard_surface` has no `anisotropic_vdf` anywhere in its graph, so the old `subsurface_present && !medium_present` guard was true for every one of them: **every transmissive `standard_surface` in the gallery was filled with the default subsurface medium**, a dense lossless scattering interior nobody authored, and the Collective Project's glass eye was rendering as frosted glass. The medium now travels on the `BSDF` struct and is propagated by the same selection that propagates `sampledL`, because the question is not what a material published but what interior the lobe that carried the path encloses, and only the combinator that chose that lobe knows; `layer_vdf` is the one place it travels the other way, since that node is how MaterialX says a surface encloses an interior. The entry is a cosine lobe into the surface, and that is a recorded decision rather than an oversight: an index-matched interface does not deviate a ray, but a straight-through entry is a delta with no solid-angle density to report, and every surface model puts this node inside a `mix` whose combinator must report a mixture density. Both were implemented and measured on a sphere of a 0.6 material in a unit furnace -- undeviated 0.5577, cosine 0.6173, at 0.2 they read 0.1684 and 0.2093 -- and both conserve energy exactly, so the choice is about where the light goes and not how much of it there is. The fourth defect was found by the colour gate: van de Hulst's relation is steeply nonlinear where subsurface materials live, and applying it to three numbers *before* they became a spectrum made a magenta material render 0.5178, 0.1971, 0.4158 for an authored (0.6, 0.2, 0.4) -- a seventh short in red with the channels either side moving the other way. The reflectance is now published unconverted and inverted per wavelength, and the same material reads 0.6139, 0.2052, 0.4072, matching the achromatic probes to within the noise. Carrying that meant the medium travels as an extinction and a bounded colour rather than as two coefficients, since two coefficients resolved to wavelengths separately do not keep the ratio between them and that ratio is the whole colour of the material; what the colour means travels with it, because `anisotropic_vdf` describes an interior by a coefficient pair and `subsurface_bsdf` by the light that comes back out. Two adjacent errors had to be fixed to get there. The path state conflated "which side of the interface am I on" with "is there an interior to transport through" in one hardcoded bit, and a clear dielectric answers those differently -- it encloses nothing and still has an inside, which is the side a closure reads its relative index from -- so they are now separate; conflating them the other way took `layer(R, T)` from 0.9978 to 0.9955 by undoing the relative-index fix. And `hdclaude_lane_extinction` upsampled a coefficient's transmittance over one *scene unit*, so a coefficient of 30 arrived as 1e-13 and the clamp returned 18.4: every medium denser than about 18 per unit mapped to the same one, and a subsurface material is nothing but such media. It is now referred to the medium's own mean free path, which is the best-conditioned band the fit has and is the same answer at any scene scale. The gates: a closed sphere of unit-albedo subsurface reads 1.0018, 0.9984, 0.9983, the slab carrying the asset's own zero-radius component reads 1.0109, 1.0157, 1.0139, and the colour gate reads 0.6173 for 0.6 and 0.2093 for 0.2, with a three-hundredths tolerance that separates the shipped boundary from the identity mistake at 0.19 and from the undeviated entry at 0.5577. Five gallery scenes moved and each is explicable: Collective Project 001 by 0.0328 as its glass eye stops being frosted, the bubblegum ball by 0.0727 over 57 per cent of its pixels as subsurface transports for the first time, the chess set by 0.0148 as its stone bleeds like marble, the Playground by 0.0191 as its green jar and purple toy transport, and honey by 0.0351 as its albedo of (1.0, 0.552, 0.229) survives being resolved to wavelengths. Sponza, the gold and glass balls, the Kitchen Set, the subdivision matrix and the height map are byte identical, which is the attribution: nothing without an interior moved. Written up in [implementation-notes.md](implementation-notes.md). Absorption is carried on shadow rays: a shadow ray that starts in an interior -- the path's own, toward a light on the side it came from, or the one a transmission lobe enters, chosen under evaluation exactly as the sampling pass would choose it, and entered by the same geometric-normal test the scatter uses -- delivers its light attenuated by `exp(-sigma_t * d)`, which is what the scattered path's walk returns in expectation for a flight with no collision. A light authored with `shadow:enable` false is exempt, since no object blocks it and nothing establishes where along the line the interior ends; attenuating them over the whole distance took 0.31% of the OpenPBR Playground's light in a first version, most of it on the octopus, and was found by marking the affected shadow rays per material. It only matters for a light inside the medium, and there it mattered in full: a light one unit into a slab absorbing three quarters per unit read 0.3141 of the clear slab through a rough interface, and reads 0.2448 against 0.25 at normal incidence, the smooth interface reading 0.2499 both before and after. Alongside it, a light hit after a walk that scattered no longer takes the MIS weight of the surface the path entered by, since no strategy samples a light from a scattering vertex; a light facing away inside a scattering slab lost 39% of its light to that weight (6.78 against 11.10), and since a light's density grows as its area shrinks, quartering the area at constant power moved the image by a factor of 0.809, where it now moves it by 0.990. Next-event estimation samples the transmission closure as well as the reflection one, so a light seen *through* a refracting surface is estimated rather than left to a scattered ray that happens to point at it: a smooth dielectric with a rect light behind it renders 1.9242, and a 0.02-roughness one 1.9240, against the closed form's 1.9200 for `(1 - R(0)) * L`. Measured honestly, it did not reduce the noise on the Standard Shader Ball's glass at all -- 0.1072 against its own converged image at 64 samples both before and after -- because that glass is near-specular at 0.01625 roughness, so the balance heuristic correctly gives next-event estimation almost nothing on *either* branch. It pays where transmission is rough, and the gap it closed was real; it is not what makes this asset quiet. Dispersion is transported. The wavelength-to-index relation is a two-term Cauchy whose coefficients are fully determined by the nominal index and the Abbe number, reproducing BK7, SF11 and diamond at the d-line and their F-to-C spread to six decimals, and asserting the direction a smaller Abbe number spreads in, which is the sign error no image betrays. It cannot travel through the closure -- MaterialX 1.39.3's `open_pbr_surface` declares `transmission_dispersion_scale` and `transmission_dispersion_abbe_number`, threads both into the generated function's signature and never reads either, and `ND_dielectric_bsdf` has no dispersion input to receive them -- so it is read off the document instead, resolved to one effective Abbe number `V / scale` since OpenPBR's scale multiplies the dispersive power, and pushed to the shading dispatch, which is already per material and so carries a per-material value for nothing. A path meeting a dispersive interface keeps its hero lane at that wavelength's index and terminates the other three, because a path is one direction; the survivor is scaled by the lane count, since the film divides the packet by it, which makes the estimator the unbiased single-wavelength one and four times noisier. The compensation must happen exactly once and nothing else on a path records that it has -- three zero lanes are what a terminated path looks like too -- so the path carries a flag; without it a ray entering a slab and leaving it is four times too bright. Both failures are caught by a furnace rather than by an eye: a closed dispersive slab that absorbs nothing must still render one and still render neutral, and reads 0.9919, 0.9969, 1.0061. The magnitude is asserted against a closed form, because the visible effect is a faint fringe: a smooth RT dielectric mirrored back at a rect light reflects `R(lambda)` and transmits the rest into blackness, so at n = 1.5 and Abbe 20 the pixel reads 0.0759, 0.0811, 0.0876 against the integral's 0.0782, 0.0816, 0.0874, with blue over red at 1.154 against 1.118 -- the ratio asserted separately, since an inverted sign still tints a highlight and still matches a closed form computed with the same inverted sign. The limit is that only a lobe that transmits is dispersed, which is what the input's own name says and all that can be said safely, because a reflection-only dielectric is indistinguishable at runtime from a coat -- a different interface with no Abbe number of its own. MaterialX builds OpenPBR's specular lobe as `layer(reflection-only, transmission-only)`, so an `open_pbr_surface` glass refracts a full spectrum and reflects an untinted highlight: 0.9934 blue over red against the flat control's 0.9981 over a 41-pixel window. An earlier entry here recorded a partial tint of 1.037, which was one nine-pixel window's noise -- eight independent windows averaged 0.9990 on the same code -- and the test had asserted it; energy is unaffected, since the layer hands its base `1 - F` whatever the base's index. Two gallery scenes moved slightly and neither shades differently: `hdclaude_dispersed_ior(ior, 0, lambda)` returns its argument by an early return with no arithmetic at all, but `ior` is now assigned rather than folded, and a different schedule of the same arithmetic reroutes a handful of paths -- OpenChessSet by an RMS of 0.00049 and Collective Project 001 by 6.1e-05, both means unchanged to six digits. Superseded prerequisites: the closure ABI's `hdclaude_wavelengths` was wired to a fixed quartet rather than to the path's own hero wavelengths and is fixed, and `BSDF::spectrum` is still written by nothing and read by nothing, which dispersion turned out not to need. The renderer does **not** produce non-finite samples; the earlier claim that it did was a mis-invocation, and the renders containing them were made without `--colorCorrectionMode disabled`, so usdrecord applied an sRGB transfer function to a scene-linear AOV and a negative sample raised to a fractional power became a NaN. The flag now lives in `render_claude.bat` beside `--disableCameraLight`, since the sRGB transform belongs to the EXR-to-JPEG conversion and nowhere else, and every gallery image re-renders byte-identical with it moved. What stands from that investigation is the *gate's* blindness: it compares display-transformed JPEGs, so nothing the display transform normalises -- a non-finite value, a negative, any magnitude above one -- can reach it, and the EXR is the actual rendered data. `hdClaudeImageDiff` gained a `--scan` mode that reads one linear image and reports its true range, mean, non-finite count and negative count. It says two things at once. Negatives are in ten of the eleven scenes -- 24637 in the subdivision matrix -- and are **not** a defect: a spectral renderer resolving to scene-linear sRGB gives a negative component to any colour outside that gamut, so they are reported and not failed on. And the **OpenPBR Playground's linear render has a mean of 1.17e19, a range of [-1.31e24, 2.18e25] and three non-finite samples**, in a scene whose displayed mean is 0.38 -- the display transform clamps it to white and the gate has passed it for as long as it has existed. The scan's magnitude histogram places it: 7970 samples over 1e2, 6709 over 1e4 and 1534 over 1e8, forming a dense cluster on the scene's **dark green bottle** -- its transmissive object -- with ordinary fireflies scattered elsewhere. It is long-standing rather than recent: rebuilt as it stood before the spectral MIS it reaches 1.19e23, and before the relative-index fix 8.33e22, so neither this week's changes caused it, though both reduced it -- 13543 samples over 1e2 became 7970. **Fixed.** The bottle's coat authors `coat_roughness 0.33` with `coat_roughness_anisotropy 1`, and OpenPBR's own mapping is `alpha_y = (1 - anisotropy) * alpha_x`, so its alpha pair is exactly `(0.154, 0)` -- rough in one axis and a delta in the other, by specification, with nothing wrong in the asset. hdClaude clamped that to `M_FLOAT_EPS`, 1e-8, and the GGX density carries `(h.y / alpha_y)^2`, so at that width its value is set by the last bits of the half vector -- which the sampler and the evaluator reach by different routes, the VNDF and `normalize(L + V)`. They disagree by orders of magnitude, `f / pdf` stops cancelling, and the estimator returns the rounding. The floor is now 1e-4, chosen from float32 rather than from taste: `h.y` carries an absolute error near 1e-7, so the ratio stays of order one above about 1e-6 and this leaves two decades of margin, while alpha 1e-4 is a perceptual roughness of 0.01 and far below what any sampling here resolves. It is a representability limit rather than an approximation, and whether a lobe is a delta is decided separately from the *unclamped* input so a mirror stays one. Believed because two unrelated interventions agree: substituting the asset's anisotropy to zero and raising the floor give mean 0.2378 and 0.2376, range [-3.99, 84.848] and [-3.99094, 84.848]. The scene falls from a mean of 1.17e19 to 0.2376, and **every gallery scene now scans clean with no non-finite samples anywhere**. So the scan is wired into the gate, on the linear EXR and before the display transform, since that is the actual rendered data and the transform sanitises exactly what the scan looks for; the JPEG comparison stays as the visual baseline it is. The fix moves the Playground by an RMS of 0.053 and everything else by 5e-4 or less, which is paths rerouting rather than shading changing. The same series shows the glass ball's mean converging upward with path length, 0.6859 at eight bounces to 0.7547 at sixty-four, which is what correct total internal reflection costs in path length and is why the gallery's image is darker than the converged one; the contract stays at eight, since it is a comparison standard and every scene has been measured at it. The Henyey-Greenstein sampler had its sign inverted: the published form gives the cosine against `wo`, the direction pointing back along the ray, and hdClaude built its basis around the direction of travel, so a `g` of 0.8 scattered backwards and a `g` of 1 -- which OpenPBR defines as fully forward -- sent every ray exactly back. Negating is the whole correction, since the density is symmetric under `(g, cos) -> (-g, -cos)`. It is gated by a slab with a light behind it, which reads 4.5005 forward against 2.0637 backward and reads those two numbers swapped on the old code -- a direction no furnace could catch, since both media are lossless and differ only in where the light goes. No gallery image moves: nothing in it authors an anisotropic medium that reaches the walk. Remaining: the upsampling happens at the closure boundary rather than per texel inside the MaterialX graph, so a material's own colour arithmetic is still RGB; hero-wavelength MIS, which would keep a dispersive path's other three lanes alive instead of terminating them; next-event estimation at a scattering vertex, whose only path space is an emitter inside the same closed interior; a path inside a *clear* interior that reaches no boundary is still killed as an unclosed medium, which was right when every transmission was a medium and is now merely conservative; the walk's 256-collision cap is reached rather than avoided by a material whose mean free path is small against the object, which biases the exit point and not the energy; light-power selection, IES profiles, light filters, geometry lights |
| 7 | Hydra delegate: adapters, render pass, AOVs, settings | In progress | plugin discovered as "Claude GPU Path Tracer"; `UsdPreviewSurface` networks shade through MaterialX's own `ND_UsdPreviewSurface_surfaceshader` rather than falling back to `displayColor`, which is 137 of Intel Sponza's materials; mesh, material, light, camera, instancer and render-buffer adapters -- point-instanced and natively instanced geometry is placed by hdClaude's own `HdInstancer`, since the stock one computes nothing and `GetInstancerTransforms` returns the instancer's transform rather than its instances, and an instanced rprim is placed by *both* its own transform and its instancer's -- the first takes the mesh into the prototype, the second the prototype into the world, and using the second alone collapses every mesh of a model onto that model's origin, which is what had scattered Pixar's Kitchen Set and read as a refrigerator, a stove and a table that were simply absent; progressive render pass; all three `gallery/shader_ball_*.usda` scenes render lit and materially distinct; textures load through Hio and bind bindlessly, and the interpolated texture coordinate reaches the material -- asserted by a material whose albedo is its own UV -- including a face-varying one, read from the prim's primvar descriptors and triangulated per corner, which is how a UV seam and a textured quad are both authored. textures arrive the way round they were decoded, asserted by a four-quadrant image whose corners must land where they are named; an image node with no file reads its declared default rather than the failure placeholder; and a material reading UVs through `geompropvalue` gets them. a `<UDIM>` token expands to the first tile the resolver finds, and every component type Hio decodes is accepted, so a 16-bit mask and a set authored on tile 1003 both load; an input whose name or type the linked MaterialX does not declare is dropped with a warning rather than costing the material; and a `UsdPrimvarReader` is rewritten into the `geompropvalue` it wraps, whose `geomprop` MaterialX otherwise cannot read through the nodegraph interface. Face-varying texture coordinates survive refinement, through an OpenSubdiv face-varying channel rather than as vertex data, which is what keeps a seam a seam. A float or half source keeps its range in `R16G16B16A16_SFLOAT` rather than being quantised into eight bits: quantising an integer source loses precision and is a fair trade, while quantising a float source *clamps* it, and an HDRI's whole contribution as a light is the window and the sun above 1.0 that clamping removes. A texture's colour space comes from the material rather than from the file format: `HioImage` answers from the bytes alone and calls every 8-bit three-channel image sRGB, which is right for a colour map and wrong for the normal, roughness and metalness maps shipped in the same JPEGs, so the `colorspace` attribute MaterialX authors, the `sourceColorSpace` input `UsdUVTexture` authors, and failing both the image node's own output type are each read, and the decision is cached alongside the path. A `<UDIM>` set is expanded to one image per tile and the tile is chosen *per sample* from `1001 + floor(u) + 10*floor(v)`, which it has to be: ALab's `discPin_M_geo` straddles tiles 1002 and 1013 on its own, so a choice made per mesh or per material would still be wrong on that prim. A filename in this target is therefore a handle -- first slot, tile-table offset, tile count -- rather than a `sampler2D`, which carries one image and cannot express a choice among many; the six `image` nodes are overridden for that body alone, and `HwImageNode` is registered under the genglsl_pt names because it supplies the `uv_scale` and `uv_offset` inputs the nodedef does not declare. The tiles are asked of the asset resolver before generation, since the count decides how many array slots the set takes, and a set is not required to be contiguous or to start at 1001 -- the turntable jumps 1007 to 1013 and the Playground's tools start at 1003. A sample on a tile the set does not ship reads the node's `default` rather than a neighbour, and one inside a tile is held half a texel from its edge -- the shared sampler addresses `REPEAT`, which is right for a map and wrong for a tile, so a fetch just inside a boundary used to come back with the opposite edge of the same tile and lay a one-texel band of the wrong thing down every seam; clamping from the texture's own size is what `CLAMP_TO_EDGE` would do and needs no second sampler. Filtering still does not cross tiles, which needs border texels replicated from the neighbour at load time and is recorded rather than guessed at. The loader now refuses an unexpanded token instead of collapsing it, because collapsing is what was wrong: every tile was shaded with the set's first existing one, which put 53.5 per cent of ALab's `electronics_turntable01` under tile 1001's image with nothing to say so. Gated by a quad spanning six tiles against a set that ships three -- 1001, 1002 and 1012 read their own primaries and 1003, 1011 and 1013 read zero, where the old behaviour would have made all six red. It moved the OpenPBR Playground by an RMS of 0.0322 over 32 per cent of its pixels, and nothing else in the gallery beyond a firefly. Remaining: a second UV set, primvar geomprops beyond position, normal, tangent and UV, per-texture wrap and filter modes, depth and id AOVs, and the type mismatches that prune Intel Sponza's normal, roughness and metalness maps, which are *not* hdClaude's to coerce: MaterialX's specification allows a connection only between the same type (its one documented exception is `string` to `filename`), so dropping the input with a warning is the spec-correct behaviour and stays. The mismatches are upstream defects and are recorded as such in [implementation-notes.md](implementation-notes.md): hdMtlx's USD-to-MaterialX type table has no entry for `normal3f`, `vector3f` or `point3f`, so an authored `normal3f` value becomes a *string*; a connected input takes its type from the upstream output, and MaterialX's own `ND_UsdUVTexture` declares `rgb` as `color3` while its own `ND_UsdPreviewSurface_surfaceshader` declares `normal` as `vector3`, so the wiring the USD specification prescribes for a normal map is a type error by MaterialX's own rule; and that table maps USD `float4` to `vector4` where the same nodedef declares `scale` and `bias` as `color4` |
| 8 | Gallery correctness against the specifications | In progress | `render_gallery.bat` renders, times, display-transforms and gates every scene, and rewrites the timing table; `hdClaudeDisplayTransform` and `hdClaudeImageDiff` are the two tools behind it, and the gate fails on RMS, worst pixel, failed-pixel count, a black image, or a non-finite sample. All ten scenes render, with baselines, timings and the machine record ([gallery.md](../gallery.md)); each names what its image still gets wrong. **Correctness here is defined by the specifications** -- USD, MaterialX, the spectral model, and the physics of path tracing -- and by applying their rules as written. It is not defined by agreeing with hdCodex. Some of that renderer's images are themselves wrong, so a difference from one is a question rather than a defect: the work is to analyse what differs, decide from the specifications and from closed forms which renderer is right, and record the assessment. Where hdClaude is right and hdCodex is not, the difference is the correct outcome and stays. Chasing agreement with another renderer's output would be the same error as loosening a tolerance until a test passes -- substituting a plausible reference for an actual standard -- and this project already prefers a closed form to a previous render everywhere else. Where a specification is genuinely wrong, incomplete or silent, that is a finding to record explicitly, as it already is for MaterialX's dropped dispersion inputs and hdMtlx's type table, and the decision taken in its absence is justified from physics and written down. hdCodex stays useful as an independent second opinion that says where to look -- its image of Pixar's Kitchen Set is what revealed that hdClaude's committed baseline had been wrong from the day it was adopted -- and is never what settles the question. Sponza is far darker than hdCodex's and that has not yet been analysed either way. Remaining: that analysis, scene by scene, against the specifications rather than against the other renderer. The renderer-owned instance placement scene exists: `usd_instancing` builds its stage and its flattened reference together, so its expectation comes from USD's API rather than from a previous render, and it carries a negative control that proves the comparison can see a dropped mesh transform. **Open, and not caused by the work that found it: OpenChessSet does not reproduce.** Its render differs from its committed baseline by rms 1.6e-3 with a worst pixel of 0.53 against a limit of 0.3, on 0.026% of the frame -- a handful of pixels, which is the shape of a firefly rather than of a changed image. It is also not deterministic: two consecutive runs of one binary differ from each other by rms 3.7e-4 with a worst pixel of 0.28. Checked out at `ad57ff5` and rebuilt, it fails in exactly the same way, so it predates every commit of 2026-09-21 and the baseline has been stale or the render flaky since before them. The baseline is deliberately **not** accepted: accepting an image that the next run will not reproduce would turn the gate off for that scene. What it needs is the source of the nondeterminism, which a deterministic path tracer should not have |
| 9 | Interactive contract: frame identity, per-slot resources, real overlap | In progress | The single frame-scoped entry point lands: `BeginFrame(FrameDescription)` / `EndFrame(FrameHandle)` replaces `Render(width, height, camera, settings)`, and `InvalidateFor` is the only thing that reads the description -- extents, camera, settings, mode and scene revision decided once, which is the arrangement the 2026-09-05 decision named and hdCodex's setter triad is the argument for. A caller's `resetAccumulation` is an input rather than the answer: it says what the caller believes and the renderer decides what it knows, so a caller that forgets a resize, a mode switch or a scene revision still gets a correct frame. `FrameResult` carries the extents it was rendered at, its index and its sample range, and the render pass checks those against the buffer before writing -- the check whose absence produced findings A2, A1, N1 and N8; it cannot fire until something overlaps, and is there so it is already right when something does. `EndFrame` on a taken handle returns nothing rather than the previous frame, and the gate asks twice. Gated on the invalidation itself: every case asks for `resetAccumulation = false` and is right to be overruled. All eleven gallery scenes re-render byte identical at RMS 0, which is the property that makes the step attributable. `bounce` has moved out of the frame uniform onto a push constant on the two kernels that vary with it, which is the prerequisite for recording more than one bounce into a command buffer: the uniform is host-written, so a field that varies per bounce forces a submit between every pair of them. **Was blocked, and not on itself; the block is now lifted -- see the resolution at the end of this entry.** Batching a whole sample into one command buffer takes the render suite from 243 s to 107 s with synchronisation validation clean, and it appeared to move the Open Chess Set by an RMS of 0.0018 -- but the same unbatched build then moved four gallery scenes by between 3.7e-5 and 9.4e-4, including the New Zealand height map, which is one textured quad that nothing in the change can reach. Rendered twice more against its committed baseline it reads exactly 0 both times. **The renderer is intermittently nondeterministic**, two orders below the gate's RMS limit, so the gate has never failed on it and a genuine change of that size is indistinguishable from nothing. Every remaining step of this phase -- per-slot resources, then real overlap -- rests on the argument that the image did not move, which a renderer that cannot reproduce its own output cannot support. So the batching is reverted rather than shipped on an unverifiable claim, and the next task is an instrument: render the same scene twice in one process, compare the linear films, and repeat enough to catch something usually invisible. The instrument exists and passes: the render suite renders one scene progressively, four accumulating chunks of eight samples, and compares the resolved films *exactly* six times over, then six more times with the scene republished between renders so the acceleration structures are rebuilt -- 0 of 6 and 0 of 6, worst 0.000e+00. The reproduction is across *processes*: three `usdrecord` runs of the Open Chess Set give three different linear films, rms 0.00131 and 0.00054 with worst pixels of 1.218 and 0.314 over some 0.006 per cent of pixels. Four causes are eliminated -- the scene store's ordering (`std::map<SdfPath, ...>`, so a light's index cannot vary and neither can NEE's selection), the generated MaterialX (all sixteen programs byte identical between two processes), repetition within a process, and rebuilding the acceleration structure within one. It is driven by sample count and not resolution: 1024 x 1024 at 64 samples is identical across three processes and at 1024 samples differs in every pair, which puts it near one ray in a hundred million and explains why every in-process gate passes at 128 x 128. Four more causes are eliminated -- the progressive machinery (the same total samples in one call and in thirty-two are both irreproducible), uninitialised path state (every path buffer is now fillable with a known pattern by `HDCLAUDE_POISON_PATH_STATE`, and poisoning does not make it reproducible), the sort's scatter (both writes are `atomicAdd`), and resolution itself. An earlier reading that the effect was *bimodal* is withdrawn: it rested on two runs producing the same figure, and three runs differ from each other by 1.06e-5, 2.08e-4 and 5.30e-4, which is a spread. A one-bounce against eight-bounce bisect is also retracted, having been run below the threshold where anything differs at all. The cause is now **located**: a **hit hash** -- an `atomicAdd` over every hit the call resolves, keyed on the instance, the primitive and the path that found it, carried in the counters buffer and folded on the host -- reports a different value in each of three processes. A per-pixel id AOV would have been the wrong instrument, testing a million primary hits against an effect near one in a hundred million; a hash tests every hit in the render. It first showed that three processes disagree on how many rays they traced at all (993843050 / 993842838 / 993843257), and then, at **one bounce**, where camera rays are identical by construction and `tracedRays` confirms it to the unit at 640679936 in all three, that the hashes still differ. Identical rays, different geometry: **shading is exonerated**, and every later difference is downstream of a first-bounce hit that already differed. Subgroup and shared-memory effects die with it (the kernels use neither) and so does any commit-order policy in the ray query, which initialises with `gl_RayFlagsOpaqueEXT` and so sees no candidate intersections. A companion **ray hash** over the origin and direction bits then found the rays **bit-identical** across processes, which rules out the driver compiling raygen differently and leaves the acceleration structure. The chess set then went quiet -- twelve consecutive clean processes across four binaries, including three with the per-ray atomic removed to check the instrument was not masking it, which it was not -- and that was read as the fault being *intermittent at the scale of hours*. **That reading is retracted.** Every one of those twelve processes rendered the same scene, the only scene the hunt had ever used, and the conclusion generalised from it to the renderer. Arming the committed `.stats` with both hashes found the reproducer immediately: **Pixar's Kitchen Set gives a different hit hash on every render** -- 18048153105721151987, 9348002138563568471, 10304544561685832220, 13107609297471498173 -- with `tracedRays`, `shadowRays` and `rayHash` unmoved. The image gate never saw it, because that comparison is against the **JPEG** and the difference is below 8-bit quantisation; an image gate reading exactly zero is not evidence that a render reproduced. **The cause is then settled by the repeat diagnostic**: three renders of the Kitchen Set inside one process are identical in every field, so the fault is stable within a process and differs only between them. A transient fault -- a soft error on a card with no ECC -- would strike within one too. What differs is state built once at startup, and of that only the **acceleration structure** decides which triangle a ray hits: the rays are bit-identical and the tree they are traced against is not the same tree. Vulkan permits this, and says so by omission and by explicit licence -- nothing in its acceleration-structure chapter promises a reproducible build, and a candidate at `t = tmax` *may* be committed or dropped, the intersection test *may* run in floating point with inaccuracies expected, and edge watertightness is a **should**. A kitchen is full of ties; a chessboard is not. So **cross-process bit-equality is not a property this renderer can have**, and the phase does not need it: what its remaining steps rest on is that a change did not move the image, and that argument is made by rendering twice in one process, which is exactly the property now demonstrated. An in-process test at gallery settings was also **retracted as vacuous** -- `usdrecord --frames 1:4` wrote four identical films fifty-four milliseconds apart because `InvalidateFor` correctly declined to reset an accumulation nothing had changed, so frames two to four never rendered. Two things come out of it that last. `HDCLAUDE_REPEAT_RENDERS=n` renders a finished image n more times in one process and reports each render's counts and hashes, which is the only way to hold the acceleration structure, the shader compilation and the process fixed while asking whether a render reproduces; the hook sits where convergence is declared, because a host stops calling `Execute` the moment `IsConverged` answers true and a hook on the already-complete early return is never reached. And both hashes now reach each scene's committed `.stats`, so the next occurrence arrives as a diff on a tracked file rather than as a hunt -- the committed `chess_board.stats` reads a `tracedRays` nothing at the time noticed was wrong. **The batching is re-landed and verified**: one command buffer per sample holding every bounce and the film, 288 submits per call down to 32, and the render suite measured back to back at **237.84 s unbatched against 100.33 s batched**. What was missing the first time was a way to tell a regression from noise, and the hashes supply it -- the batched build reproduces the *unbatched* build's `hitHash` and `rayHash` bit for bit on the chess set, the glass ball and Intel Sponza, which a hazard could not do; three renders of Sponza in one batched process are identical; the suite passes with synchronisation validation on; and the images are unchanged at rms 0. One Sponza render moved at rms 1.5e-3 and four more returned the committed values exactly, which is the acceleration-structure fault and not the change. **Per-slot resources land**: everything a frame writes -- path state, queues, counters, the sort's table and indirect commands, the frame uniform, both readbacks -- moves into one of two `FrameSlot`s, with the film deliberately left shared because progressive accumulation is the one thing consecutive frames are meant to share. The descriptor sets are the half that mattered and were the actual obstacle: they were allocated inside `Trace` from a pool **reset at the top of every call**, which is undefined behaviour the moment a previous frame's command buffers still reference those sets, and is correct today only because every submit waits for the device -- the very property the next step removes. Sets are now allocated once per slot and rewritten only when a `_resourceGeneration` bump says a resolution change or a published scene has moved what they name. Behaviour-neutral and verified so: the suite passes, and four gallery scenes render at rms 0 with both hashes unmoved, which a 32-call progressive render exercises by alternating slots 32 times. It is also faster -- the render suite 100.33 s to 77.27 s, since allocating and writing every descriptor set on every trace was never free, and 3.1x against the 237.84 s before the batching. Two attempts at the ground work for the split are **reverted, and recorded**. Recording a whole frame into one command buffer -- 32 submits down to 1 -- is correct but markedly *slower* (chess 22.5 s to 31.6 s, glass 27.6 s to 41.8 s), because a frame is some two and a half thousand commands and the device idles for all of the recording; fewer submits was never the mechanism, removing the stall after each one was. And moving `sampleIndex` to a raygen push constant, which whole-frame recording needs, makes the **first render of every process** disagree with every later one -- four trials, always render 1 alone, not the acceleration structure (which is fixed per process) and not uninitialised state (poisoning does not change it). Reverting restores six identical renders across two processes. What the split actually needs is a **dynamic uniform offset**: one buffer with an aligned copy per sample, bound per sample, so nothing is rewritten while it is read and no kernel signature changes. **Deferred, by decision, not by neglect**: the first-trace divergence is left open and phase 10 taken up instead. The plumbing is shipped and inert; the switch is three lines when the cause is understood. Remaining: that, then the submit moves into `BeginFrame` and the wait into `EndFrame`, and only then can GPU timestamps show frame N+1's trace overlapping frame N's readback across a queue |
| 10 | Temporal foundation: jitter, motion vectors, guides, history reset | In progress | The inputs a reconstruction backend needs. **History reset** is shipped: `FrameResult::accumulationReset` reports it and `InvalidateFor` decides it from a resize, a mode switch, a scene revision or the caller's own request, which is the same event a reconstructor must discard history on, and it is documented as a renderer-side decision rather than a caller's. **Specified jitter** is shipped and applies only in `Interactive`: `Reference` accumulates hundreds of samples and a per-sample random offset is right for it, while a reconstructor must be told where the frame's one sample landed, so the offset is a frame-indexed radical inverse in bases 2 and 3, reported on `FrameResult`. **Depth** is shipped: `RenderCamera` carries the host's `worldToClip`, a guide kernel writes `(ndc.z + 1) / 2` after the first bounce's `extend` -- following hdEmbree rather than inventing a normalisation -- and the render pass finally writes the depth AOV it has always declared, which closes a phase 7 item. **Motion vectors** are shipped, and are rigid per instance: the hit is taken back through `worldToObject` and forward through the instance's previous placement, so an object that moved contributes its own motion and not merely the camera's, and the scene store keeps the previous placements because only it sees publications in order. Instance counts that changed report no history rather than pairing placements that are not the same placement. The gate is closed-form: a still camera measures 9.4e-07 px, and a camera stepped 0.1 units sideways from a quad four units ahead moves it 3.200 px against the 3.200 that trigonometry gives without rendering anything. **The hazard this phase was blocked on was not real.** Four descriptor-layout changes appeared to make the renderer disagree with itself inside one process, and the cause was the driver's shader cache: a pipeline the driver has not finished optimising is both slower and *numerically different*, and adding a binding recompiles every kernel. Proved symmetrically -- clearing `%LOCALAPPDATA%\NVIDIA\GLCache` diverges 4 runs of 4, restoring it is clean 6 of 6 -- so `HDCLAUDE_REPEAT_RENDERS` is still the right instrument but its first run after any shader change is a cold-pipeline artefact and not evidence. **The closures report the guides.** `BSDF` carries a diffuse albedo, a specular albedo, a normal and a roughness, written before branch dispatch so every pass publishes the same values (asserted bit-identical), with definitions taken from NVIDIA's DLSS-RR Integration Guide: the conductor's specular albedo reads 0.6771 against an integrated 0.6770, and a half mix of a conductor with white diffuse reads diffuse 0.5000 exactly. **The integrator writes them.** `shade` asks the material once more on the first bounce, with a fixed sample value, and writes the primary surface's normal and linear roughness, diffuse albedo and specular albedo into one guide buffer that reaches `FrameResult`; misses and analytic lights keep NVIDIA's sky defaults. Closed-form: a quad turned 0.6 rad reads the normal (0.56464, 0, 0.82534) against sin and cos of 0.6, white 0.8 diffuse reads albedo 0.800 and roughness 1, and a dielectric of alpha 0.3 reads roughness 0.5477 against sqrt(0.3) -- in a one-bounce render, where a guide read from the scattering pass would not exist. The pass consumes no random numbers and taking it changes nothing: with it compiled in and never taken, the subdivision scene's ray hash equals the hash with it taken, and both differ from before only because the kernel compiles differently. **And Hydra can ask for them**: `normal` (Hydra's own token, world-space), `diffuseAlbedo`, `specularAlbedo` and `roughness` are AOVs with NVIDIA's sky defaults as clear values, written untouched by any transfer function, under depth's rule that they describe the traced grid; a pass that binds only a data AOV now renders, where it used to return for want of a colour AOV. UsdImagingGLEngine lists `normal` among the delegate's AOVs, so usdview offers it; it probes only a fixed list, so the other three are reachable by name and not from its menu. **Not yet verified in a host**: the suites are USD-free and usdrecord writes only colour, so the pixels a Hydra buffer receives are unmeasured. Remaining: that measurement |
| 11 | Renderer-native reconstruction fallback (any Vulkan device) | Not started | - |
| 12 | NVIDIA NGX bootstrap and runtime support query | Complete | The SDK fetches, configures and builds: `compile.bat dev-dlss` reports `NVIDIA DLSS ........ ON`. That took fixing two defects in `cmake/NvidiaDLSS.cmake` that had stood since it was written, because the preset had never once been invoked -- `FetchContent_Populate` was wrapped in a `block()`, whose new variable scope left `dlss_SOURCE_DIR` empty, so the SDK was declared unobtainable in the message immediately after it cloned successfully; and the library path named `lib/Windows_x86_64/x86_64`, a directory this SDK has never had, against the `x64` it ships. A missing library is now fatal rather than a downgrade: an SDK that cannot be obtained is a fact about the machine, and an SDK present with headers and no library is a fact about that file being wrong, and treating the two the same is how the first defect survived. The support query answers: on an RTX 5060 Ti it reports DLSS available with both super resolution and ray reconstruction, with validation clean, and NGX is shut down again so nothing is left running. Three more corrections were needed and each was hidden by the one before: the cmake linked `nvsdk_ngx_s.lib`, which is the *static*-CRT build against hdClaude's `/MD` -- read out of the libraries rather than the naming, since `_d` carries MSVCRT directives and `_s` LIBCMT -- and that is the third defect in that file and the second that only a link could find; NGX will not initialise without the instance and device extensions it names, which is what `VulkanRequirementProvider` was designed for and had no implementation of, so the provider is now installed before the context is constructed; and one of those extensions, `VK_EXT_buffer_device_address`, cannot be enabled at all, because Vulkan 1.2 promoted it to a core feature hdClaude already turns on and the specification forbids both -- the validation gate caught that on the first run. `NVSDK_NGX_VULKAN_Init_with_ProjectID` also validates its project ID as a UUID and answers `FAIL_InvalidParameter` for a name, which is indistinguishable from every other way that call can fail. The boundary holds: `include/hdclaude/gpu/reconstruction.h` names no NVIDIA type, `src/gpu/ngx_support.cpp` is the only translation unit that includes an NGX header, and `hdClaudeNgx` is linked PRIVATE. Both configurations pass all seven suites -- `dev` answers "this build has no NVIDIA DLSS SDK" and `dev-dlss` answers with the capabilities -- and a `dev-dlss` test preset now exists, which it did not. The `ReconstructionBackend` interface now exists and DLSS runs behind it. It departs from the sketch in docs/dlss-integration.md 2 in three ways, each written up there: `Resize` takes a command buffer, because DLSS's feature creation is recorded rather than immediate; `QuerySizing` replaces `QuerySupport`, because support is already answered by `CreateNgxBackend` returning null with a reason and what a live backend is actually asked for is the render extent; and Vulkan types cross the boundary, which the rule never forbade. Two more facts about DLSS were found the only way they can be, by running it: the optimal-settings call reads a callback that only the *capability* parameter block carries, so allocating a fresh one answers `FAIL_OutOfDate` from a perfectly current driver; and the output image needs `VK_IMAGE_USAGE_TRANSFER_DST_BIT` as well as storage, because DLSS clears it itself -- which validation caught on the first evaluation. Phase 12 is done |
| 13 | DLSS Super Resolution and DLAA | Complete | DLSS runs inside the path tracer. An interactive frame that asks to be reconstructed is traced at the extent the backend asked for and comes back at the extent the caller asked for: 128x128 to 256x256 on Performance, DLAA at 256x256 answering its own output extent as its render extent, finite everywhere, brightest component 11.05, validation clean, and the quality-mode change rebuilding both the feature and the images. The film and the two guides reach it through `shaders/reconstruct_inputs.comp.glsl`, which packs them out of the buffers every kernel indexes into the images a backend reads; `film` and `guides` were left alone deliberately, so a reference frame pays nothing -- not a dispatch, not an image, not even the packing kernel's compile. `RenderSettings::reconstruct` and `reconstructionQuality` are the render setting that chooses the mode, honoured in interactive mode alone. The half of the gate that is met: reference output is bit-identical with DLSS present and absent, checked twice over. Within one process, a reference frame rendered before any backend exists and again with NGX initialised and the backend alive compares equal under `memcmp` -- byte for byte, because "untouched" has no epsilon. Across builds, `dev` and `dev-dlss` both hash that film to `8e46df46f3ff2e79` on an RTX 5060 Ti. Both configurations pass all seven suites: 16700 checks in `dev`, 82244 in `dev-dlss`. One defect this found, and it was in neither the backend nor the path tracer: `HdClaudeRenderDelegate` built its Vulkan context without `NgxRequirementProvider`, so NGX's own push-descriptor layouts failed validation and the backend reported that the *device* could not run DLSS. It is unfixable from where that answer is read -- the extensions can only be enabled while the instance and the device are being created -- and the delegate and the render tests now install the provider unconditionally. The other half now closes too. `include/hdclaude/core/image_metrics.h` adds SSIM and a temporal-stability measure with no Vulkan and no OpenUSD, so their closed forms are checked on any host -- an image against itself, against a scaled copy, against a blur, against noise, and a sequence whose flicker is 0.4 by construction. DLAA at native resolution over twenty-four frames of one sample each reads SSIM 0.3755 against the converged reference where the unreconstructed frame reads 0.0154, and temporal instability 0.0252 where the sequence it was made from reads 0.7265. Both are stated as comparisons against the same estimator at one sample rather than as thresholds somebody chose, so neither can be tuned without making the renderer worse, and a backend that returned the reference itself is excluded by asserting the reconstruction is still short of it. The jitter's sign is settled and was an error: hdClaude's offset says where the sample landed, DLSS's says how the projection was moved, and the two are the same displacement seen from opposite ends, so the value handed to NGX is negated (Programming Guide 3.7.2, 3.7.3). SSIM could not have found that -- on this scene it reads 0.6675 aligned and 0.6655 a whole pixel out -- so a third metric answers where a picture sits rather than how good it is: `EstimateShift` solves the Lucas-Kanade normal equations on a bilinear warp, recovers shifts known in closed form to about 0.002 px, and separates the two signs by a factor of ten. Holding the offset still at half a pixel on each axis, the reconstruction sits 0.1302 px from the converged render as shipped and 1.3934 px with the sign reversed, against a gate of 0.25. Reconstruction is now reachable from Hydra: `Reconstruction` and `Reconstruction preset` are render settings, also readable as `HDCLAUDE_RECONSTRUCTION` and `HDCLAUDE_RECONSTRUCTION_PRESET`, and asking for one is what puts the render pass into interactive mode -- verified through `usdrecord` on the gold shader ball at 512 px, DLAA at 512x373 and Performance upscaling 256x187 to it, with the preset selecting between DLSS's F, J and K. `HDCLAUDE_DLSS_RUNTIME_DIR` puts a different runtime in front of the renderer and the version travels in the backend's name (`NVIDIA DLSS 310.3.0.0`), because the model is the DLL and NGX will not report it. What this exposed, and it is a finding rather than a defect: DLAA over a *one-sample* path trace keeps only 70% of the converged image's mean, and 97% at sixty-four samples a frame, so the loss is DLSS rejecting fireflies as outliers rather than anything in the plumbing -- confirmed by the assertion that the near-converged case keeps 99.8%. Still outside this phase: the render pass does not overlap frames, which is phase 9's remaining work and a performance claim rather than a correctness one |
| 14 | DLSS Ray Reconstruction | Complete | Unblocked by phases 4 and 10. The NGX backend builds and evaluates Ray Reconstruction's own feature beside Super Resolution's -- `ReconstructionModel` chooses, a rebuild like the preset, and `Reconstruction model` / `HDCLAUDE_RECONSTRUCTION_MODEL` exposes it with Super Resolution the default -- fed the colour, depth and motion Super Resolution takes plus the three guides the MaterialX closures report, packed by the same kernel into images, with roughness in the normals' alpha, hardware depth, and no exposure input, which Ray Reconstruction does not support (DLSS-RR Integration Guide 3.7). Its presets map by what each model is: transformer to E, transformer-alt to D, and `stable`, which names a Super Resolution model it does not have, builds the default and is reported. **The gate holds on an RTX 5060 Ti.** A reference render is byte-identical after Ray Reconstruction has run -- the film hash is 8e46df46f3ff2e79, as it was at phase 13. Over 24 frames of one sample each against a 512-sample reference, Ray Reconstruction reads SSIM 0.7653 where the unreconstructed frame reads 0.0151 and Super Resolution 0.3590, flicker 0.0025 against 0.7355 unreconstructed and 0.0258 for Super Resolution, and keeps 103.1% of the converged image's light where Super Resolution keeps 88.9%. Asserted as better than the unreconstructed frames and short of the reference, not as thresholds; the comparison with Super Resolution is printed. The run found that switching model at an unchanged extent rebuilt the backend's history without the frame reporting a reset, now fixed. **And the specular hit distance** the Integration Guide asks for when specular motion vectors are not given (3.4.9), with the world-to-view and view-to-clip matrices it is read against: `shade` records a probe at each primary hit along the view reflected about the closures' shading normal, a `specular_hit` kernel traces them against the same geometry, curves and lights `extend` does, and misses hold FP16_MAX. Closed-form: a mirror at 45 degrees reflects the centre pixel's view onto a wall 1.5 away, and the guide reads 1.44562 against the [1.4375, 1.5] its pixel's footprint allows. The matrices are hdClaude's column-major ones unconverted, since NVIDIA's row-major, row-vector convention -- translation in row 3, as Streamline's matrix helpers build a camera -- is the same sixteen numbers |
| 15 | ~~SER in `extend`~~ | Deferred | Not possible: SER builtins do not exist on the compute stage, and the per-material sort already provides the coherence. See [implementation-notes.md](implementation-notes.md) |
| 16 | GPU displacement through the generated MaterialX program | In progress | Two pieces so far, both in the MaterialX layer and both gated without a GPU. The generator produces a second program from a material's `displacement` terminal: the graph terminates in a `displacementshader` rather than a `surfaceshader`, and that is what it keys on, because MaterialX classifies the `displacement` constructor as a shader node exactly as it classifies `surface` and only the output type separates them. The entry point takes no parameter -- a displacement graph has no closures to sample and so no ClosureData to thread -- and leaves its answer in `hdclaude_displacement`, a struct declared for every material rather than only the displacing ones, so the ABI does not depend on the document. A displacement fed by a varying pattern generates, names `hdclaude_material_displace`, writes the struct, carries no shading entry point, and compiles to SPIR-V: 2993 bytes of GLSL and 985 words. The second piece reads from the document what generation erases. `AuthoredDisplacement` answers with the terminal a `surfacematerial` connects and the frame that terminal's offset is in, because MaterialX's two constructors emit the same two lines and mean different things by them, and what separates them is the declared type of their `displacement` input -- float or vector3 -- rather than the category, which is `displacement` for both. The terminal travels with the answer rather than being found again by the caller: the generator has to be handed it by name, since `findRenderableElements` answers with surfaces and a material that only displaces is not one. A `dot` between the material and the constructor is followed, which the specification licenses as a no-op routing point, and a `displacement` node no material connects is ignored as the unreferenced node it is. What has no answer in the specification is reported rather than guessed at: a `mix` of a scalar and a vector displacement, whose result is in no declared frame, and a second material that displaces differently from the first. 113 checks pass in the MaterialX suite where 79 did, and all eleven suites pass. The third piece is the geometry the program will be evaluated against. `ComputeVertexFrames` answers, for each vertex of a prototype, with the normal, the texture coordinate and dP/du and dP/dv, accumulated over every triangle that shares the vertex and weighted by twice each triangle's area. Per vertex rather than per triangle because a vertex is displaced once: one answer per incident triangle would move the same vertex to several places and open the mesh along every edge. The derivatives are left unnormalised and not orthogonalised, so the kernel can build the frame exactly as `shade.comp.glsl` builds it and a displacement and a shade of the same material agree about which way the tangent points. Checked against closed forms on a quad whose parameterisation makes dP/du exactly (2, 0, 0): the mirrored island turns it, a mesh with no coordinates falls back to the barycentric parameterisation the shade kernel uses and stays in the surface, a UV seam keeps the first coordinate authored for the vertex rather than a mean that appears nowhere on the map, a crease's two face-varying normals sum to the direction between them, and a curve answers with nothing. 92 checks pass in the geometry suite where 46 did. The fourth piece runs it. `shaders/displace.comp.glsl` is the geometry counterpart of `shade.comp.glsl`: appended to a generated *displacement* program rather than a surface one, one invocation per vertex rather than per path, and reading the prototype's buffers as device addresses pushed in a constant, because a displacement runs once per prototype and there is no per-frame descriptor set to put a prototype's geometry in. The one descriptor it declares is the shared texture array, at the slot the generated material itself declares. `DisplacementPass` owns the pipeline and dispatches it, and the displaced positions come back to the host, so a displaced prototype is an ordinary prototype and the fingerprint, reuse and refit machinery is untouched. The gate is arithmetic rather than a picture: on a quad whose parameterisation makes the frame the identity basis, a scalar displacement of 0.25 at scale 2 puts every vertex exactly 0.5 along +z, a vector displacement of (0.5, -0.25, 0.125) at scale 3 moves each axis by its own amount -- so a frame assembled in the wrong order cannot pass -- and a height driven by the u coordinate moves the two vertices at u = 0 not at all and the two at u = 1 by the whole scale, which a program evaluated once and copied cannot do. The worst error in all three is 0.000e+00. One defect this found, in the generator rather than in any of it: the `SurfaceHit` setter was emitted only for a material that reads some geometry, which every surface graph does because the surface node registers the position and the normal itself, and no displacement of a constant does. It is now always emitted, empty when there is nothing to set, so the ABI no longer depends on what the document happens to read; the material ABI revision is 2. 16924 checks pass in the render suite, and all eleven suites pass. The fifth piece closes the loop, and the gallery had been waiting for it. `HdClaudeMaterialCompiler` builds a second MaterialX document from the network's `displacement` terminal -- hdMtlx builds one from whichever terminal node it is handed, and `Document::addMaterialNode` names the material's input after the shader node's type, so a displacement terminal arrives connected as `displacementshader` -- generates from the terminal `AuthoredDisplacement` found, joins the displace kernel, and carries the result on `CompiledMaterial` beside the surface program. Its textures are their own table: the two programs generate separately and each numbers its samplers from zero, so a height map is index 0 of the displacement whatever the surface reads, and the pool is still one pool. A displacement that fails to compile does not take the material with it -- the surface is still the surface as authored, and an undisplaced mesh with a message saying so is the honest consequence, not a grey stand-in for shading that was perfectly good. `PathTracer::SetScene` then runs the pass before the acceleration structures, over the prototypes whose material displaces, caching both the pipeline and the displaced positions so a republication that changed neither the geometry nor the material does not run the program again. Two gallery scenes had authored a displacement that hdClaude ignored, and both now render it. `newzealand_heightmap` was a flat quad with a map of New Zealand painted on it; it is now terrain, with the Southern Alps standing out of the plane. `subdivision_features`, whose own comment promised "bilinear displacement", tilts its magenta quad by the ramp `u - 0.5` scaled by 1.1, which is what that graph says. Both baselines are replaced; the other nine scenes are unmoved, six of them bit-identical. One thing the displacement made necessary and one measurement of it. The necessary part: a displaced surface's normal is the displaced surface's normal, and the array the mesh carried describes where the surface used to be, so the normals are recomputed from the moved positions. The measurement: on the height map that is worth rms 4.1e-4 and at most 0.037 in any pixel, which is small because the terrain is mostly flat and not because the normals are ignored -- forcing every one of them to (1, 0, 0) moves 8.6% of the frame, where recomputing them moves 0.004%. 16938 checks pass in the render suite and all eleven suites pass. Still to come: a render against a sphere of known radius, and the residency question below, which the host round trip answers only provisionally |
| 17 | External-memory AOV interop for Hgi hosts | Not started | - |

Phases 13 and 14 are the two hdCodex never reached. They are deliberately late:
each depends on guide buffers that are only meaningful once phases 4-10 are
correct, and feeding a reconstructor from a broken estimator is explicitly a
non-goal.

## Exit gates

Each gate is a claim that can fail. "The code exists" is never a gate.

**Phase 0.** `compile.bat` configures and builds the core-only preset from a
clean clone with no manual dependency steps. `setup_usd_env.bat` reports a clear,
actionable failure for each missing dependency rather than failing later inside a
tool. All committed documents cross-reference consistently.

**Phase 1.** Unit tests pass with no GPU and no OpenUSD present. Spectral round
trip: sRGB primaries and a set of Munsell reflectances upsample and re-integrate
to within 1e-3 dE2000. The shader cache is proven to miss on every component of
its key.

**Phase 2.** A validation-enabled run of the full suite reports zero validation
errors **and the test fails if the count is nonzero** (rule R8). Validation here
means core *and* synchronisation validation: core validation checks that each
command is legal on its own and says nothing about whether what one kernel wrote
is visible to the next, which in a wavefront integrator is most of what can be
wrong. Device-loss
simulation leaves the latch set, every subsequent entry point refuses, and the
destructor completes without issuing a wait.

**Phase 3.** Prototype BLAS reuse, in-place TLAS update, and in-place BLAS update
under stable-topology deformation are each asserted by reading back GPU state,
not a CPU shadow (rule R7). Subdivision preserves creases, corners, holes,
orientation, face-varying seams, and material subsets against a committed
baseline -- and the baseline is not enough on its own. From 2026-09-06 to
2026-09-07 this gate passed against a baseline in which the creased cube was
fully rounded, because a baseline can only prove that nothing changed. The
gate is met when the claim is also asserted by something that does not
compare the renderer against itself.

**Phase 4.** All 22 pbrlib overrides present (the set is all-or-nothing; see
[materialx-codegen.md](materialx-codegen.md) §3), and the five acceptance items
in §8 of that document: every gallery material compiles with no fallback, white
furnace within 0.5%, chi-squared agreement between sampling and the reported
density, a finite nonzero density at every sampled direction, and combinator
mixture densities that integrate to one — for every closure **and every
combinator**. The last item is the one that catches a combinator reporting the
selected child's density instead of the mixture.

**Phase 5.** Per-material dispatch is observed to scale: adding a large
procedural nodegraph to one object changes that object's shading cost and not
the frame's. Queue counters are GPU-written and never read back by the CPU
during a frame.

**Phase 6.** Furnace test at scene scale. Analytic light comparisons for each
`UsdLux` type. Spectral: a dispersion scene matches an analytic reference; a
metamer pair renders as distinct under two illuminants.

**Phase 7.** A render-pass-level test drives move -> move -> stop -> move against
a stub scene and asserts a frame reaches the AOV on every moving frame (rule R7;
this is the test whose absence hid hdCodex A2/A1/N1/N8). Failure paths assert
`IsConverged()` stays false and the revision is retried.

**Phase 8.** Every gallery scene renders, and the image gate in the gallery
script passes against a committed baseline (rule R9). Timings, GPU, driver, and
TDR values recorded (rule R11). Every remaining difference from hdCodex is
*assessed against the specifications* and its resolution recorded -- which
renderer the specification agrees with, and why -- rather than closed by making
the images match. A difference that hdClaude is right about is a passing result.

**Phase 9.** GPU timestamps prove frame N+1's trace overlaps frame N's readback —
measured across a *queue*, not within one command buffer, since a whole-buffer
barrier's first synchronisation scope includes everything already submitted to
the queue. This is the exact claim hdCodex could not make (finding N3); it is
achievable here only because of Rule 1.

**Phase 10.** Jitter sequence, motion vectors, and history reset are validated
against a synthetic scene with known motion. Motion vectors are correct under
instancing and deformation, or the phase does not close.

**Phase 11.** The native fallback improves a fixed-sample interactive image
measurably (SSIM against the converged reference) on a device with no DLSS.

**Phase 12.** Runtime support query returns a correct answer on a supported and
an unsupported device, and the renderer runs unchanged when DLSS is absent.

**Phase 13.** DLAA at native resolution is compared against the converged
reference by SSIM and by a temporal-stability metric. Reference mode output is
bit-identical with DLSS present and absent.

**Phase 14.** Ray Reconstruction fed only by guides produced by MaterialX
closures (never by a surface-model name). Reference mode remains unaffected.

## Decision log

Decisions are recorded when made, with the reason, and are not silently
reversed.

| Date | Decision | Reason |
| --- | --- | --- |
| 2026-09-05 | Wavefront integrator, not a megakernel | A megakernel must contain the union of all material code; with real compiled MaterialX programs that destroys occupancy frame-wide ([architecture.md](architecture.md) §2) |
| 2026-09-05 | `VK_KHR_ray_query` in compute, not `VK_KHR_ray_tracing_pipeline` | In a wavefront design the SBT buys nothing already gained by separating shading from traversal, and costs portability plus a second shader-compilation path |
| 2026-09-05 | MaterialX `genglsl_pt` derived target, not a lowered closure ABI | Full MaterialX fidelity including arbitrary pattern and procedural graphs; the documented extension mechanism (`targetdef inherit`) |
| 2026-09-05 | Four hero wavelengths, not three | `vec4` is the natural GPU register width; better hero MIS; and the interactive preview must not change estimator (hdCodex N9) |
| 2026-09-05 | Separate reference and interactive storage | Sharing one buffer caused four hdCodex findings at once ([lessons](lessons-from-hdcodex.md) C1) |
| 2026-09-05 | Single `BeginFrame(FrameDescription)` entry point | Independent setters caused five hdCodex findings ([lessons](lessons-from-hdcodex.md) C2) |
| 2026-09-05 | CPU readback is the Hydra AOV adapter; interop is a later phase | No public OpenUSD API crosses a device boundary for a `VkImage`. Decided now because phases 11 and 17 both depend on the answer (hdCodex D1, which was never decided) |
| 2026-09-05 | DLSS via NGX directly, not Streamline | A delegate does not own presentation, so Frame Generation and Reflex — Streamline's reason to exist — are out of scope |
| 2026-09-05 | Film and shadow accumulation use no atomics, by invariant | Paths map one-to-one onto pixels, so exactly one invocation writes each entry. Tracing several paths per pixel would need `VK_EXT_shader_atomic_float`; the invariant is stated in the shader so the requirement is not discovered by a race |
| 2026-09-05 | Acceleration-structure reuse is keyed on a geometry fingerprint | Not on prototype index or name: a publication that reorders or renames prototypes must reuse everything, and one that changes a vertex must rebuild only that prototype. The opacity class is part of the fingerprint because it changes the build flags |
| 2026-09-05 | Traversal is asserted by hit pattern, not hit count | A structure built at the wrong scale, or with a transposed instance transform, still produces hits. Coverage fractions and per-pixel instance identity are what distinguish those from correct traversal |
| 2026-09-05 | The density invariant includes discarded probability mass | A visible-normal sampler can produce directions below the horizon, which the closure cannot scatter into. The reported density integrates to one *minus* that mass, and asserting a bare integral of one would have driven a real bug into correct closures ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-05 | Shader and surface nodes thread `closureData` too | Replacing the surface node's calling convention means a shader *nodegraph* must pass it through, or its generated function references an undeclared identifier ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-05 | Subsurface and volume closures publish parameters; the integrator transports | A random walk and volumetric absorption happen along paths inside a medium, not at a surface. Mirrors MaterialX's own OSL target, which emits a closure and leaves transport to the renderer |
| 2026-09-05 | MaterialX 1.39.3 is the version of record | It is what OpenUSD 26.03 ships and what hdClaude links; 1.39.6 differs in headers, closure signatures, throughput semantics, and helpers ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-05 | `ClosureData` stays byte-compatible; extra state in globals | 1.39.3 constructs it inline with a fixed argument list, so adding fields breaks every construction site hdClaude does not replace. A global is per-invocation in a compute shader, so it costs nothing |
| 2026-09-05 | The implementation declarations are generated from the stock ones | Nodedef names do not follow file names, and a wrong one fails silently by falling back to the stock implementation. Deriving them removes the class of error |
| 2026-09-05 | Screen-space derivatives are never used | Neighbouring lanes in a `shade` dispatch are unrelated paths, so `fwidth` measures nothing. Footprints come from ray differentials |
| 2026-09-05 | The validation layer is built from source, not assumed installed | Validation is a gate the GPU tests fail without, so it is a dependency rather than an optional local install. Pinned to the same SDK tag as the headers, loader, and compiler ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-05 | Dependencies by pinned CMake `FetchContent`, never committed | A fresh clone reproduces the tree; the repository stays small and carries no third-party or proprietary code |
| 2026-09-05 | Closure sampling returns a direction only; densities are written by the evaluation closure types | MaterialX evaluates a combinator's children *before* the combinator, so a density returned by sampling belongs to that child's own direction and cannot be mixed. Moving densities to evaluation lets every combinator mix them with the weights it already mixes responses with ([materialx-codegen.md](materialx-codegen.md) 2) |
| 2026-09-05 | The `surface` node gets its own genglsl_pt implementation | `HwSurfaceNode` constructs `ClosureData` inside a rasteriser light loop and an environment lookup. A path tracer supplies its own direction and light sample, so the calling convention has to be replaced alongside the closures ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-05 | SER is not used | Its builtins exist only on ray-tracing-pipeline stages, not compute, and the per-material sort already delivers the coherence SER recovers ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-05 | On device loss, skip the wait but still destroy objects | `vkDestroyDevice` requires its children to be destroyed first, and destruction stays valid after loss; skipping it faults. Only the unchecked wait contaminates diagnostics |
| 2026-09-06 | The environment is an emitter, and the only one MIS applies to | A scattered ray can reach the environment and cannot reach an analytic light, so the environment needs a weight against BSDF sampling and the lights would be halved by one. Its density is uniform over the sphere because the environment kernel has to recompute it from a direction with no surface in hand ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-06 | `UsdPreviewSurface` is shaded through MaterialX, not refused | MaterialX declares and implements it as a nodegraph, so generating it is the same path as every other material and involves no extractor. Refusing it was applying the no-approximation rule to a case that is not an approximation |
| 2026-09-06 | Screen-space derivatives are defined as zero, not forbidden | The rule is unchanged -- a derivative between two unrelated paths measures nothing -- but a path tracer's filter width at a shading point genuinely is zero, so an inherited node that asks for one should evaluate rather than fail to compile. `mx_subsurface_scattering_approx` stays deleted, so the case that is a real approximation still fails loudly ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-06 | An asset's version drift is tolerated; its shading is not | Refusing to approximate a material is about shading fidelity. An input whose name or type the linked MaterialX does not declare is a version difference in the asset, and dropping it with a warning costs one input rather than the whole scene |
| 2026-09-06 | The closure type follows the sampled direction's side | The integrator, not the closure, knows whether a sampled direction crossed the surface. Evaluating a refraction with the reflection closure asks about a direction below its horizon, and the zero density that comes back is indistinguishable from an impossible sample ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-06 | A structure's identity is what it owns, not what the build reads | `BottomLevelStructure` serves the normal and UV buffers to the shading kernel, so two prototypes with identical geometry and different shading data are not interchangeable however identical their builds would be ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-06 | An image node with no file reads its default, not the placeholder | The magenta placeholder means "this asset names a file that cannot be read". A node with no file is an ordinary authoring choice with a defined MaterialX answer, and rendering it as a glaring failure colour misreports the asset |
| 2026-09-06 | The generated geometry setter assigns every member, never itself | A member left unassigned is undefined, not "filled later by the kernel": the struct is a global and nothing else writes it. The self-assignment that stood for "the kernel knows this one" made every object-space pattern and every texture coordinate read undefined memory ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-06 | Instance transforms are read through one helper, not inline | They are stored row-major 3x4 for Vulkan's instance structure, and GLSL indexes a `mat3x4` by column, so the obvious construction yields the transpose. One `hdclaude_linear` keeps that reasoning in a single place instead of at each use, where it was wrong twice |
| 2026-09-06 | Synchronisation validation is part of the validation gate | Core validation passed a whole frame whose counter reset raced the copy that read those counters. The class of defect the gate exists to catch is exactly this one, so the layer setting is enabled in the context rather than left to whoever remembers an environment variable ([implementation-notes.md](implementation-notes.md)) |
| 2026-09-06 | The prefix sum runs in one invocation, not a parallel scan | The key range is the scene's distinct compiled materials -- tens -- and a serial scan of tens of integers costs less than the workgroup barriers a parallel scan needs to be correct. It shares a kernel with the indirect-argument writes, which read the same counts |
| 2026-09-07 | The analytic lights are hittable, and intersected in closed form rather than tessellated | A light nothing can hit cannot appear in a mirror or a glass ball at any sample count, because a delta closure skips next-event estimation entirely and has no second way to find one. Closed form rather than triangles because a rect and a sphere *are* closed forms: tessellating them would add a discretisation of a shape already known exactly, plus a build, plus a question about how finely to divide a light nobody is looking at. The cost is a loop over the lights per ray, which is right for the handful a scene authors and wrong for a thousand; the density and the emission do not change when that day comes, only where the intersection comes from. **Amended 2026-09-10**: still hittable, but only when `RenderSettings::lightGeometry` asks for it, and it does not by default -- a light usually stands in for a fixture the asset also models. What this decision says about a mirror remains exactly true and is now the documented cost of turning the shapes off |
| 2026-09-07 | Subdivision tags are read from the scene delegate, not assumed | `GetMeshTopology` returns the cage and says nothing about how to refine it; the tags are a separate `GetSubdivTags` call. Skipping it does not fail loudly -- the refiner falls back to OpenSubdiv's defaults, which disagree with USD's on the boundary rule (`edgeAndCorner` versus none), on creases and corners (present versus absent), and on face-varying interpolation (`cornersPlus1` versus fully linear). One missing call read as three unrelated bugs: shells split open along closed seams, creased models rounded off, and textures moved at every UV seam |
| 2026-09-07 | The stand-in sun lights a stage that lights itself in no way at all, and is aimed by a declared up axis | A dome light is not an entry in the light table -- it supplies the environment -- so counting the table alone called a dome-lit stage lightless, and the Open Chess Set had been rendering under its own HDRI *plus* a second key light nobody authored. And elevation is measured about an up axis that a Hydra scene delegate is never told: usdImaging passes the world as authored and there is no token for it, so it is a render setting whose default is Y. Getting it wrong does not dim a scene, it points the sun sideways, which is what had been happening to the Z-up Kitchen Set |
| 2026-09-07 | Invalid or mismatched scene input is reported, never coerced | A renderer that silently patches malformed input hides the defect from the person who can fix it and makes its own image untrustworthy, because you can no longer tell which pixels came from the asset and which from the workaround. The limit of the rule is documentation: where a specification says two things are interchangeable, supporting the equivalence is correctness rather than magic -- and MaterialX's specification says the opposite here, allowing a connection only between the same type with one exception, `string` to `filename`. `tools/check_usd_materials.py` is the reporting half of the decision, checking a stage at the USD level so a finding is about the asset rather than about any renderer |
| 2026-09-07 | A colour temperature is transported as a spectrum, and replaces the illuminant rather than multiplying it | An RGB tint is a metamer of the blackbody, so it lights a surface whose reflectance varies across the spectrum -- every real one -- differently from the light it stands for, and `enableColorTemperature` is the input where transporting four lanes most obviously earns them. The two illuminants are equated on their luminous integrals rather than their peaks, because a 2700 K blackbody peaks well outside the visible range and matching peaks would make a warm light a dim one |
| 2026-09-08 | The NGX support query ships before the backend interface it belongs to | `ReconstructionBackend`'s Resize, Evaluate and ResetHistory describe work phases 10 and 11 have not produced anything for. Declaring them now would leave three unimplemented virtuals standing in for a phase that is not done, so phase 12 ships the two things that are complete -- what NGX needs before the device exists, and what it can do once it does -- and the interface grows in with the native backend |
| 2026-09-08 | A UDIM tile is chosen per sample, and a filename is a handle rather than a sampler | The tile a sample reads follows from its own texture coordinate, and one prim can straddle two tiles -- ALab's `discPin_M_geo` does -- so the choice cannot be made per mesh or per material. A `sampler2D` carries one image and cannot express it; a handle carrying the set's first slot and its tile table can, at the cost of overriding the six `image` bodies |
| 2026-09-08 | A sample on a tile a UDIM set does not ship reads the node's default | Not the nearest tile and not the first one. There is no image there, and substituting one is the same mistake as the behaviour being replaced -- which shaded every tile with the set's first existing image and said nothing |
| 2026-09-08 | An interior medium travels on the `BSDF` struct, selected with the direction | Both `standard_surface` and `open_pbr_surface` instantiate `subsurface_bsdf` and `anisotropic_vdf` whether or not the material uses either, so a global records whichever closure ran last. The path enters the interior belonging to the lobe that carried it through the interface, and only the combinator that chose that lobe knows which that was. Every transmissive `standard_surface` in the gallery was being filled with an unauthored scattering medium until this changed |
| 2026-09-08 | A subsurface colour is inverted per wavelength, never per channel | van de Hulst's relation, which OpenPBR quotes to define `subsurface_color`, is steeply nonlinear, and a nonlinearity applied to an RGB triple before it becomes a spectrum does not commute with the projection back. Inverting the three numbers first left a magenta material a seventh short in red; upsampling the reflectance and inverting each lane makes the walk return the authored colour |
| 2026-09-08 | A medium is carried as an extinction and a bounded colour, not as two coefficients | Absorption and scattering resolved to wavelengths through separate fits do not keep the ratio between them, and that ratio -- the single-scattering albedo -- is the whole colour of a subsurface material. What the colour means travels with it, since `anisotropic_vdf` authors a coefficient pair and `subsurface_bsdf` an observed reflectance |
| 2026-09-08 | The subsurface entry is a cosine lobe, not the undeviated direction | An index-matched interface does not deviate a ray, but a straight-through entry is a delta with no solid-angle density, and every surface model puts this node inside a `mix` whose combinator must report a mixture density; the alternative is a narrow lobe of some invented width. Measured both: on a 0.6 material the undeviated entry reads 0.5577 and the cosine entry 0.6173, both conserving energy exactly |
| 2026-09-08 | A coefficient's transmittance is upsampled over the medium's own mean free path | At a fixed unit distance a coefficient of 30 arrives as a transmittance of 1e-13 and the clamp returns 18.4, so every medium denser than about 18 per unit mapped to the same one -- which is every subsurface material. Referring it to `1 / peak` puts the densest channel at `exp(-1)` and is the same answer at any scene scale |
| 2026-09-08 | Being inside an interface and having an interior are separate facts | They were one hardcoded bit, and a clear dielectric answers them differently: it encloses nothing and still has an inside, which is the side a closure reads its relative index from. Conflating them took `layer(R, T)` from 0.9978 to 0.9955 by undoing the relative-index fix |
| 2026-09-09 | Motion is rigid per instance, and a deformation reports the rigid part only | A mesh whose *points* changed has moved in a way no matrix describes. Reporting the transform's motion is honest about what it measures; synthesising a per-vertex correspondence to fill the gap would invent history a reconstructor would then fetch |
| 2026-09-09 | An instance with no previous placement reports no motion, not identity motion | An unset `previousTransform` is the identity, and believing it would say every surface had just flown in from the origin. The absence is carried explicitly as `hasPreviousTransform` rather than inferred from the value, because the identity is also a legitimate placement |
| 2026-09-09 | `Snapshot` is not const: it records itself as the next snapshot's past | An rprim republishes whenever anything about it changes and has no idea which publication is which; the store sees them in order and is the only thing that does. A snapshot is a point in a sequence rather than a pure view |
| 2026-09-09 | A new member of a host-side aggregate goes last, never in the middle | Brace elision let `{0, Transform3x4{}, 1, true}` keep compiling with its material index flowing into the first float of the new transform, and thirty-seven call sites silently lost their materials with no diagnostic. Appending cannot do that; the ordering is a correctness property, not a style |
| 2026-09-09 | `Resize` takes a command buffer and may fail | DLSS's feature creation is recorded into a command list rather than executed immediately, and it can decline. A signature taking extents alone, as the design sketch had it, cannot express either, and the caller would have had to discover both by watching `Evaluate` do nothing |
| 2026-09-09 | Vulkan types cross the reconstruction boundary; NVIDIA types still do not | Images and command buffers are how any GPU reconstruction is expressed, and wrapping them would buy nothing that the rule was written to buy. The rule is that `NVSDK_NGX_*` appears in one translation unit, and it does |
| 2026-09-09 | DLSS is asked for the *capability* parameter block, never a freshly allocated one | The optimal-settings call reads a callback that only the capability block carries. An allocated block answers `FAIL_OutOfDate`, which reads as a driver too old and is nothing of the kind |
| 2026-09-10 | A separate kernel packs the film and the guides into images; `film` and `guides` are untouched | A reference render must be bit-identical whether or not a backend exists, and the surest way to keep it so is for the kernels that produce it never to learn that reconstruction exists. The cost is one dispatch on an interactive frame that is being reconstructed and nothing at all on every other frame |
| 2026-09-10 | The render extent is the backend's answer, and travels on the `FrameResult` | DLSS chooses what to trace per quality mode, so an upscaled frame's image and its guides are different sizes. Reporting `renderWidth`/`renderHeight` beside `width`/`height` is what stops a caller reading the depth AOV at the image's stride and walking off the end of a correct buffer |
| 2026-09-10 | A backend that fails leaves the frame as it was traced, rather than failing the frame | An optional backend that turned its own absence into a failed frame would make the renderer's behaviour depend on the machine. The result says `reconstructed` is false and `ReconstructionUnavailable()` says why, which is a preview at the wrong size and never a black image |
| 2026-09-10 | `NgxRequirementProvider` is installed unconditionally by the Hydra delegate | NGX will not initialise without instance and device extensions of its own, and those can only be enabled while the instance and the device are created -- long before anything decides to reconstruct anything. Without it the backend reports that the *device* cannot run DLSS, which is untrue and unfixable from where it is read |
| 2026-09-10 | The jitter handed to DLSS is negated | hdClaude's jitter is where the sample landed; DLSS's is the offset applied to the projection (Programming Guide 3.7.2, 3.7.3), and offsetting a projection by `+d` moves the content `+d` so the sample moves `-d`. The axes need no further flip, because the guide asks for the co-ordinate system the motion vectors are in and both they and the images are in the images' own row order ([dlss-integration.md](dlss-integration.md) 5) |
| 2026-09-10 | A caller may state the frame's sub-pixel offset; the renderer chooses one only when it does not | A host already driving a temporal pattern should jitter the way that pattern does, which is what the DLSS guide asks of a renderer that has TAA (3.7.2). It is also the only condition under which a sign error in the offset is a displacement rather than a blur, so it is what made the question above answerable. A reference render still refuses one, and says so |
| 2026-09-10 | A reconstruction's *position* is measured directly, not inferred from SSIM | On a smooth scene SSIM against the converged reference reads 0.6675 aligned and 0.6655 a whole pixel out. A metric that answers the same either way cannot settle a question about where the picture sits, and a gate resting on it would have passed whichever sign was shipped. `EstimateShift` reports the displacement in pixels and separates the two answers by a factor of ten ([dlss-integration.md](dlss-integration.md) 5) |
| 2026-09-10 | Reconstruction is a render setting, and asking for it is what selects interactive mode | Nothing else in the render pass chooses between the two estimators, so a second setting for the mode would be a second answer to one question. `HDCLAUDE_RECONSTRUCTION` and `HDCLAUDE_RECONSTRUCTION_PRESET` are the same names as the Hydra settings, so a viewport session and a batch render are configured identically ([dlss-integration.md](dlss-integration.md) 5a) |
| 2026-09-10 | An unrecognised reconstruction setting is reported and refused, never guessed at | A misspelt mode and a machine without DLSS leave the same unreconstructed picture on screen, and at one sample a frame both look like a broken renderer. The pass says which, once per distinct answer, and names the backend, the extents and the preset when one does run |
| 2026-09-10 | The DLSS runtime directory is an environment variable, and the version is part of the backend's name | The model *is* the DLL: which DLSS produced a picture is a question about which `nvngx_dlss.dll` was loaded, and NGX has no call that reports it, so the answer is read off the file. Comparing two runtimes is otherwise not something the renderer can even describe |
| 2026-09-10 | The depth AOV is withheld while reconstruction upscales, rather than resampled | The guide is written at the render extent and the buffer is at the output extent, so writing one into the other reads a correct buffer at the wrong stride. The guide is real and correct and simply not an AOV at that size; stretching it into place would invent depths no ray measured |
| 2026-09-10 | DLSS loses a third of a one-sample path trace's energy, and that is the input rather than the plumbing | Measured on the gold shader ball: 70% of the converged mean survives at one sample a frame and 97% at sixty-four, with the image maximum falling 15.8 to 8.8. DLSS rejects outliers against a neighbourhood and a one-sample path trace is largely outliers. The near-converged case is the test that separates this from a defect -- at 128 samples a frame the reconstruction keeps 99.8% of the converged mean, which nothing dropping a pre-exposure could do. It is the sharpest argument for Ray Reconstruction ([dlss-integration.md](dlss-integration.md) 5a) |
| 2026-09-10 | The frame's exposure is measured and handed to the backend, not left to it | The DLSS guide is imperative -- the renderer "must provide" it in a 1x1 texture (3.9) -- and calls the parameter the preferred method against its own estimate (3.10). It did *not* fix the emissive-scene energy loss it was written for: a measured 12.5 and auto-exposure produce byte-identical output on collectiveproject001 frame 1080. It stays because the specification requires it and a backend should not guess what the renderer already knows ([dlss-integration.md](dlss-integration.md) 5a) |
| 2026-09-10 | A value handed to a backend is read back and reported, not assumed delivered | Three separate experiments on the exposure were invalidated by a stale installed shader, and every one of them read as "DLSS ignores this input" -- which is the same observation as "the plumbing is broken". A sentinel of 1234 moved the frame's mean and settled it. The exposure now travels back off the device and is printed with the frame |
| 2026-09-10 | Light geometry is a render setting, off by default, and it can only take geometry away | A `UsdLux` light usually stands in for a fixture the asset also models, and rendering the bare rectangle beside the lamp it represents puts a shape in the picture nothing meant to show. Off overrides whatever a light authored rather than combining with it, so a scene cannot put geometry into a render that asked for none; on hands the choice back per light. **UsdLux defines no per-light camera-visibility attribute** -- the only per-light visibility USD has is `UsdGeomImageable`'s, and an invisible light prim is removed entirely rather than kept as an invisible emitter -- so `Light::visibleGeometry` is one for every light today and exists to keep the override separable for the day USD names one |
| 2026-09-10 | A light whose geometry is hidden takes its next-event contribution whole | It cannot be found by a scattered ray, so there is no second strategy to share with and the balance heuristic would give away a share to a sampling event that can no longer happen -- every hidden light too dark by exactly that much. It is the treatment the stand-in sun already had, for the same reason. Asserted: a surface lit by an out-of-frame light reads a ratio of 1.0012 between the two settings, where leaving the weight alone read 0.14 |
| 2026-09-10 | Cubic curves are evaluated to polylines and swept, not refused | A B-spline passes through none of its control points -- it begins a sixth of the way into its own control polygon -- so sweeping the control cage would draw a curve the asset did not author, which is why they were refused rather than approximated. The three bases UsdGeomBasisCurves names are now evaluated with its own segment rules: a segment is four consecutive control points, the stride is the basis's vstep (3 for Bezier, 1 for bspline and catmullRom), and a vertex count that does not describe whole segments is reported rather than truncated. `pinned` wrap stays refused by name: it repeats phantom control points at both ends, which changes the segment count and every index, and drawing it as nonperiodic would leave the curve short at both ends |
| 2026-09-10 | Widths ride the same basis as the positions | They are `vertex` interpolation in every ALab curve set, so a per-point width is an attribute of the curve exactly as the position is and is evaluated with it. A constant or per-curve width means the same thing before and after and is carried through untouched. The evaluated width is clamped at zero because a cubic combination can undershoot where authored widths change sharply, and a negative radius sweeps the tube inside out |
| 2026-09-10 | A cubic segment becomes one straight span by default | One is not the same as not evaluating: it puts both ends of every segment *on* the curve, which for a B-spline is nowhere near the control polygon. Hair is authored at several segments a strand and is thinner than a pixel at any sane distance, so the spans are already shorter than the geometry they approximate, and `HDCLAUDE_CURVE_SEGMENT_SAMPLES` multiplies the swept triangle count by exactly its own value. ALab's stoat and Remi reach 122,126,052 triangles and 14.2 GiB at one sample and six sides |
| 2026-09-10 | A deforming prototype refits its acceleration structure rather than rebuilding it | A Vulkan update keeps the tree and moves its bounds to follow new vertices, which is exactly what a mesh at a later moment of an animation needs: the topology is unchanged by definition, so nothing has to be partitioned again. Reuse gains a second key -- a topology fingerprint hashing the indices, the counts and the flags but *not* the positions -- because a deforming mesh arrives with a geometry fingerprint nothing matches and a topology that matches its own previous frame. Measured on ALab: publish falls from 86 s a frame to 11.8 s |
| 2026-09-10 | Updatable structures are asked for on the *second* sight of a topology, not the first | `VK_BUILD_ACCELERATION_STRUCTURE_ALLOW_UPDATE_BIT_KHR` is not free -- the structure is larger and traverses slower -- and most prototypes in most scenes never move. The first build has no evidence the geometry deforms; the second sight of the same topology is the first moment there is any. So a static scene pays nothing and an animation pays one extra rebuild before every later frame is a refit |
| 2026-09-10 | A refit overwrites its buffers in place rather than reallocating them | The vertex, normal and UV buffers keep their allocations and take new contents. A second copy of a deforming groom is precisely the allocation that pushed ALab past the device: three frames spilled 12.4 GiB to host memory before this and spill nothing after it, which is most of where the seven-fold speed-up comes from |
| 2026-09-10 | Curves are intersected as segments, not swept to triangles | A swept tube is `sides` facets a span -- about five hundred bytes -- and approximates a circle; a segment is two positions, two radii and two strand parameters, and *is* the circle. ALab's stoat and Remi fall from 122,126,052 triangles and 14.2 GiB to 9,103,872 triangles and 4.6 GiB, and publish from 23.1 s to 2.9 s. `sides` stops existing for curves: there is nothing to choose because nothing is being approximated. `HDCLAUDE_CURVE_GEOMETRY=swept` keeps the old path, which is what makes the two comparable on one scene |
| 2026-09-10 | A segment is a round cone, not a cylinder | A strand's two ends have different widths, so the surface is a truncated cone; the sphere at each end is what makes consecutive segments join without a gap or a crease, which a bare cone leaves wherever the curve bends. The visible difference from a swept tube is exactly that -- rounded ends and an exact silhouette instead of flat ends and six facets |
| 2026-09-10 | The strand parameter is carried on the segment, not derived from it | A curve's texture coordinate runs root to tip of the whole curve. A segment that measured only itself would hand every strand a sawtooth instead of a gradient, so each end carries how far along it is -- the same `v` `SweepCurves` writes, so a material reads the same thing whichever way the curve is drawn |
| 2026-09-10 | Procedural geometry needs no shader binding table, because traversal is a ray *query* | An AABB is only ever a candidate: `rayQueryProceedEXT` hands it to the kernel, which intersects the cone and calls `rayQueryGenerateIntersectionEXT`. That is the same loop in `extend` and in `shadow` and costs no new pipeline stage, which is a dividend of the 2026-09-05 decision to use `VK_KHR_ray_query` in compute rather than a ray-tracing pipeline |
| 2026-09-10 | Curve geometry defaults to swept again while the implicit path has a known artefact | The implicit path is the better representation by every measure that was taken -- exact shape, a third of the memory, an eighth of the publish time -- and it draws a crescent at every segment joint. A known visual defect should not be what a render does by default, whatever else is true of it. It stays one environment variable away, which is also what makes the two comparable on one scene |
| 2026-09-10 | Geometry settings are render settings too, and changing one resyncs the stage | Curve geometry, curve sides, curve segment samples and the subdivision level decide what an rprim *is*, so unlike a sample count they cannot take effect on the next frame -- the prototypes were built the old way during Sync. The render pass is the only part of the delegate holding a render index, so it compares them each execute and marks every rprim dirty when one moves. They are settings and not only environment variables because the interesting thing to do with them is flip one in a viewport and look, which is how swept and implicit curves are judged against each other on a real groom |
| 2026-09-14 | Curve geometry defaults to implicit, reversing the entry above, with the joint artefact still unfixed | Paolo asked for the lighter path as the default. On ALab it is 4.6 GiB against 14.2 and 2.9 s of publish against 23.1, and on an asset whose swept form does not fit on the card that is the difference between a render and none. The crescent at segment joints remains a known defect (open question 5), and `swept` stays one setting away as what it is compared against. The delegate's descriptor and the render pass share the one default -- a pass that read an unset setting as swept rebuilt every curve on its first frame -- and a value that is neither `implicit` nor `swept` renders implicit and is reported once by name |
| 2026-09-14 | Structures a publication cannot use are released *before* its new ones are built | Building into a second map and freeing the old one afterwards holds both at once, and for a scene whose geometry has wholly changed that is two complete copies on the device: switching ALab's groom from swept to implicit asked for 14.2 GiB and 4.6 GiB together and the frame died. What survives is any structure whose geometry fingerprint is asked for again, or whose topology fingerprint is, since those are what a deforming prototype refits onto |
| 2026-09-14 | A generated intersection is compared against the ray's interval *and* the committed hit in the kernel, not left to the implementation | Vulkan's closest hit determination drops a confirmed hit with t > t_max, and the driver this renderer runs on did not: `extend` committed a farther curve hit over a nearer one, which was the crescent at every segment joint, and `shadow` let a curve beyond a light occlude it. A comparison the implementation is also meant to make costs one read of the committed t, and makes the kernel correct whichever way an implementation behaves. Measured both ways on one build: 656 differing depths without it and 0 with it; a curve beyond a light at 0.9105 of the light without it and 1.0000 with it |
| 2026-09-14 | Phase 4's chi-squared gate is asked the way the integrator asks | Sampled directions are histogrammed over the sphere against the reported density integrated per cell, and every direction is evaluated as `shade` evaluates it -- REFLECTION in front of the shading normal, TRANSMISSION behind. A harness that asked one fixed type would have agreed with closures the renderer cannot use. It asserts mass from its own quadrature, because the uniform-sphere estimate clamps each value at 64 to protect a fixed-point sum and a refracted lobe's peak exceeds that, reading 0.72 for a closure whose mass is 1.000; and it asserts a closure keeps most of what it samples, since a closure that discards everything agrees with itself perfectly |
| 2026-09-14 | A rough transmission lobe rejects back-facing microfacets | For a direction off the refracted lobe, the refraction half vector turned to the upper hemisphere faces away from the view, so no ray reaches it; Walter et al.'s BTDF is zero there through its chi-plus factors, which MaterialX's G2 does not carry, and pbrt-v4's DielectricBxDF discards exactly these. `dielectric_bsdf` and `generalized_schlick_bsdf` tested only that V and L lie on opposite sides of H and answered response and density: at 34 degrees and alpha 0.3 the density expected 10,847 samples in the 112-degree ring where the sampler produced none, chi-squared 7254 on 593. A double-precision host transcription reproduced it exactly and agreed at 581 on 577 once fixed |
| 2026-09-14 | A dielectric's reflection that leaves below the horizon, or refraction that does not cross, is discarded rather than handed back | The integrator evaluates a direction by which side it is on, so a reflection sample below the surface was weighted by the *transmission* density, which never produced it: one sample in 150 at that setting. Returning no direction is what pbrt-v4 does in both branches and what `shade` already treats as a discarded sample. Moved in the gallery at noise level only: the glass ball at rms 2e-4, BubbleGum 2.1e-3, the OpenPBR Playground 2.7e-3 spread evenly over the frame |
| 2026-09-14 | `translucent_bsdf` and `chiang_hair_bsdf` answer TRANSMISSION as well as REFLECTION | A rasteriser evaluates every light with REFLECTION whichever side it is on, so upstream answers nothing else; hdClaude's integrator asks TRANSMISSION behind the normal, which is every direction translucent samples and most of hair's TT lobe. Translucent kept none of a million samples and returned albedo 0, where white diffuse transmission is 1 by construction and now reads 0.9998; hair discarded half and returned albedo 0.033 against 0.324. Thin-walled OpenPBR subsurface is built from translucent, so the Playground's paper passed no light through at all: its sheet on the desk is now lighter and takes the wood's tone, rms 0.0138, brighter only. Asserted by a closed-form albedo for translucent and zero discards for hair, both shown to fail with the fix removed |
| 2026-09-14 | A visible-normal density uses the exact anisotropic masking of the view | Heitz's D_V(H) = G1(V) max(0, V.H) D(H) / N.V holds only with G1 of the distribution being sampled. The conductor, dielectric and generalized-Schlick densities passed MaterialX's one-roughness `mx_ggx_smith_G1` the geometric mean of the two, while the sampler stretches each axis separately: at 34 degrees along the smooth axis of a 0.2 by 0.6 lobe, G1 0.9953 against 0.9863, so every weight was 0.9% high. Three measurements moved together -- the furnace from +0.873% to -0.041%, chi-squared from p 0.0046 to 0.34, samples kept against the density's expectation from 880,185 vs 872,772 to 880,185 vs 880,756. Isotropic lobes are the same formula and unchanged. The response keeps MaterialX's own masking-shadowing, because that is the model; only the density has to be the sampler's |
| 2026-09-14 | Reconstruction guides split into diffuse and specular albedo, and describe the primary surface | DLSS Ray Reconstruction takes diffuse albedo, specular albedo, normals and roughness as separate inputs, defined in its Integration Guide; one combined albedo could not supply them. Every closure publishes all four before its branch dispatch, because a guide is a property of the surface and the view and the sampling pass must agree with every evaluation -- which also moved a transmit-only lobe's early return below the guides, since returning first left a REFLECTION pass with none, render-neutral at rms 9e-5 on the glass ball. The contract's first-non-delta rule for mirrors is replaced by the primary surface: NVIDIA asks that depth, normal and hit distance describe one surface, and a mirror is already distinguished by reporting its reflectance as specular albedo |
| 2026-09-14 | Ray Reconstruction is a second model behind the same backend, not a second backend | It is the same NGX, the same device requirements and the same images, with a different feature and three more inputs, so `ReconstructionModel` selects between features the one backend builds and a change rebuilds, as a preset does. Super Resolution stays the default so nothing that already runs changes. The packing kernel fills the guide images for both, because one kernel and one set of images is simpler than two, at three render-extent images Super Resolution does not read |
| 2026-09-14 | Validation findings entirely inside NGX are reported, not counted as failures | Synchronisation validation reports two WRITE_AFTER_WRITE hazards inside Ray Reconstruction's own Evaluate on its first evaluation (DLSS 310.9.1): a layout transition then a clear, on images NGX allocates and names `nv.ngx.dlssd.resource`, under its own `nv.ngx.dlssd.Evaluate` label. hdClaude cannot fix them and the gate cannot be allowed to pass by failing forever, so a message whose innermost label is NGX's and whose every object other than the lent command buffer is NGX-named is counted separately and printed in full every run; anything naming one of hdClaude's objects still fails. Decided by Paolo |
| 2026-09-15 | Next-event estimation from a scattering vertex is not added | A shadow ray from inside an interior can deliver light only if nothing deviates it on the way, and every interior boundary hdClaude has deviates it: a dielectric refracts, and `subsurface_bsdf` enters and leaves by a cosine lobe (decision log, 2026-09-08), so the light that leaves a subsurface material toward a lamp left the boundary in a direction chosen there, not along the line from a collision to the lamp. Connecting the two straight through would estimate a path the transport does not have. The estimate that boundary does admit -- at the boundary point, with the closure's own response -- is the one `shade` already makes when a walk arrives there. What remains is an emitter inside the same closed interior, which is real but rare and would cost a shadow ray per collision in every walk, so it is recorded rather than built |
| 2026-09-15 | A shadow ray entering an interior takes the one the sampling pass would have entered | Next-event estimation through a transmissive surface needs the interior behind it, and the evaluation types used to clear it because nothing had been selected. Choosing by each lobe's share of the *response* would be the textbook answer and would make the two MIS strategies estimate different things, because a scattered path enters the interior of the lobe that was *sampled*, which at direction `L` is lobe `i` with probability `P_i * pdf_i(L) / pdf(L)`. So each combinator chooses between its children by selection probability times density, which telescopes to exactly that, and the whole response is attenuated by the chosen interior. It is exact for every material with one interior. Two limits keep it from reaching past what it can know: entering is decided by the geometric normal, as the scatter decides it, because the path's record of its medium does not nest; and a light with `shadow:enable` false is not attenuated, because an interior's absorption is part of how an object shadows and such a light is not shadowed |
| 2026-09-15 | Every lobe choice draws its own number | The combinators and both RT splits read `hdclaude_sample_u.z` directly, on the premise that a rescaled number flows down a combinator tree. It does not: MaterialX emits children before parents, so all the choices were made from one number and nested ones coincided. A counter hashed with that number gives each choice a uniform of its own without consuming more of the path's stream. Stratification through the tree, which the rescaling was meant to keep, was never happening |
| 2026-09-15 | A thin-walled surface is a sheet: no interior, transmission straight through, and the slab's Fresnel | OpenPBR defines thin-walled as "a 2d sheet with no interior" seen identically from either side, with rays "always flipped so that incident rays enter top-down", and says the translucent base "reduces to a thin sheet of dielectric" whose reflection is "modified due to the internal bounces in the sheet". MaterialX 1.39.3's `open_pbr_surface` graph reads `geometry_thin_walled` only for subsurface: its transmission lobe still refracts, still carries the interior volume whenever `transmission_depth` is positive, and `ND_dielectric_bsdf` has no input to be told. So the flag is read off the document as dispersion is (`geometry_thin_walled`, and `standard_surface`'s `thin_walled`), travels as a push constant, and the dielectric closure shades the sheet: undeviated transmission, and the lossless slab's `2F/(1+F)` reflected against `(1-F)/(1+F)` transmitted. Where OpenPBR layers its transmission lobe under a reflection-only one, the base carries the internal bounces as `F/(1+F)` reflected and `1/(1+F)` transmitted of what reaches it, which conserves energy and leaves a coat -- indistinguishable at runtime -- a single interface. A thin-walled transmission enters no medium. Two choices fill the specification's silences and are Paolo's: a rough sheet transmits straight through with its roughness ignored on the transmitted side, since there is no closed form for two rough faces together; and `transmission_color` is left as the graph gives it, which drops the tint when a depth is authored. Dispersion on a thin-walled surface has no direction to spread and is reported and dropped |
| 2026-09-15 | Instance placement is checked against USD's own flattening, with a negative control | The gallery gate compares a render with its own committed baseline and so accepted the Kitchen Set's collapsed furniture for as long as that baseline stood. The check therefore builds its reference from the specification's API -- `UsdGeomXformCache` for the xform stack, `ComputeInstanceTransformsAtTime` for the instancer, which includes each prototype root's transform -- and renders both stages through hdClaude, so what is compared is composition alone. A comparison that passes is only evidence if it can fail, so a stage with the Kitchen Set's defect built in must fail it. It runs through the installed plugin via `render_claude.bat`, since that is the path the defect was on; the C++ suites stay USD-free |
| 2026-09-15 | The specular hit distance is measured along the specular lobe's centre, one probe a pixel | The Integration Guide defines it as the distance from the primary surface to the specular ray's hit and says nothing of which specular ray, and neither does Streamline's guide. A path tracer samples one lobe a path, so the ray its path took is diffuse at many pixels and random at all of them, where the guide warns that noise in a guide confuses reconstruction (3.5); NRD, NVIDIA's denoiser before it, takes hit distance from the specular lobe. So a separate probe is traced along the view reflected about the closures' shading normal, noise-free and defined at every surface, at one ray a pixel a sample. It is a decision in the specification's silence, recorded as one |
| 2026-09-16 | Light linking is resolved by OpenUSD's `HdsiLightLinkingSceneIndex`, inserted for hdClaude | UsdLux's `collection:lightLink` and `collection:shadowLink` reach a render delegate as Hydra categories: a light's `lightLink` and `shadowLink` parameters name one, and `GetCategories` / `GetInstanceCategories` list the ones an rprim is in. Through a scene index -- how usdview and usdrecord run in 26.03 -- nothing computes those unless a scene index does, and OpenUSD ships that one without inserting it for any renderer; hdPrman inserts it for itself. Without it every link read as "everything", and ALab's sun and dome were blocked by the 3.5 km skydome their shadow links exclude. Membership travels as one bit a category per instance, indexed by the ray query's custom index, so the number of categories is unbounded |
| 2026-09-16 | A shadow-linked light is estimated by next-event estimation alone | A shadow link names the geometry that blocks a *shadow ray* from that light. A scattered ray that reaches the light has already been stopped by whatever it met first, linked or not, so the two strategies estimate different transport and no weighting of them is right; the balance heuristic would return a mixture depending on the weights. UsdLux is silent on how a path tracer combines them. Next-event estimation is the strategy the link is defined for, and its light samplers cover every direction the light emits into, so it alone is unbiased: it takes the light in full, and a scattered ray that reaches the light from a vertex where next-event estimation ran contributes nothing. From a camera ray, a delta closure or a scattering walk -- where next-event estimation did not run -- the light arrives as before. A light link is simpler, because both strategies drop an unlinked surface alike. Measured against the unlinked scenes as closed forms: a floor seen past an excluded occluder reads 0.1806 against 0.1806 open, and past an excluded roof under a white environment 0.7977 against 0.7996 |
| 2026-09-16 | A distant light's `normalize` and `DomeLight_1`'s pole axis are implemented; the UsdLux inputs hdClaude has no transport for are reported by name | Both are defined in closed form: `UsdLuxLightAPI` gives a distant light's `sizeFactor` as `pi sin^2 theta` (`(2 - sin^2 theta) pi` past a hemisphere), and UsdImaging hands the pole axis over as `domeOffset`, applied before the prim's transform as hdPrman applies it. What has no place in a physically based transport -- `diffuse` and `specular` multipliers (UsdLux calls them non-physical), `shadow:color`, `distance`, `falloff`, `falloffGamma`, `shaping:focusTint`, light filters, a rect light's `texture:file`, a dome map in any layout but latitude-longitude, portals -- is named in the light's report when authored away from the value that makes ignoring it exact, rather than substituted for |
| 2026-09-16 | Whether this machine can run hdClaude is answered from the physical device, once | `IsSupported` built a whole `VulkanContext` to answer it, Hydra asks three times per `usdrecord`, and the delegate then built a fourth: four Vulkan devices to render a stage holding one camera, of which `vkCreateDevice` is 3.8 seconds each and everything else in a context is 51 ms. Every capability the renderer requires is a property of the *physical* device, read from `vkEnumerateDeviceExtensionProperties`, so the probe needs an instance and an enumeration -- 62 ms -- and the answer is cached because a machine's hardware cannot change within a process. What it gives up is proof that the device will create; that failure is reported by the delegate's own initialisation, which says why and leaves an empty viewport, and surviving it was always the delegate's job. An empty stage renders in 6.4 s against 20.8, the gold shader ball in 11.1 against 23.0, and the USD-level suites, which spawn a process per stage, fell by nearly half |
| 2026-09-16 | A colour used as an albedo is clamped to [0, 1] in the closure that uses it | The OpenPBR Playground's `paper` drives `subsurface_color` through a `colorcorrect` node with `gain = 4`, and `mx_translucent_bsdf`'s estimate is exactly `color * weight`, so every scatter multiplied the path by four and thirty-two bounces multiplied it by 4^32 -- a scene whose mean went 0.166, 0.181, 1.53 as the bounce limit rose. An albedo above one is a surface returning more light than reaches it, which is not a picture that converges slowly but an estimator that diverges, and the same clamp already stood in `mx_subsurface_bsdf` and on a medium's single-scattering albedo in `extend.comp.glsl`. So it is clamped where it is used, and what the asset authored is reported by a check that reads the graph, since a texture-driven value cannot be reported from the host at all. The ladder converges after it: 0.159, 0.164, 0.168 |
| 2026-09-16 | A valueless `shadow:enable` is corrected by applying the API schema the asset omitted | Three of the OpenPBR Playground's lights declare `bool inputs:shadow:enable` and give it no value, without applying `UsdLuxShadowAPI`. The documented fallback of true belongs to that API schema, so a prim which does not apply it has no fallback for the property at all: `GetAttributeFallbackValue` reports the name as absent from the prim definition. A light with no such property at all is unaffected -- `mainLamp_spill_AL` has none, arrives with no value, and is shadowed as a light should be -- but an attribute that *exists* with no value is worse than one that does not, because `UsdImagingDataSourceAttribute::GetTypedValue` zero-initialises when neither the attribute nor a schema fallback answers, and every render delegate then receives `false`. hdClaude honoured it and three lights lit the room through its walls at every bounce -- a third of the frame's light. The gallery entry applies `ShadowAPI` rather than authoring a value: that is the smaller claim, since it says only that these lights have shadow controls, and lets UsdLux's own fallback supply the rest. The render is bit-identical to authoring `true`. The renderer adds no rule of its own, because the case it would have to detect is one Hydra has already erased; what it does add is a report naming every light nothing occludes |
| 2026-09-16 | Texture quality is a cap on the longest edge, and `high` is the authored image | A stage's images are the largest thing it holds -- ALab's 6,261 come to 49.6 GB against a machine with 32 -- so the control that matters is one whose result does not depend on how large the originals were: `medium` and `low` cap each image at 1024 and 256 by repeated halving, and `high`, the default, keeps what the asset authored. Nothing is ever enlarged. Each halving is a box filter, which preserves the image's mean exactly, so a reduced dome light still delivers its whole power; an sRGB-encoded texture is decoded before averaging and re-encoded after, because sRGB is concave and averaging encoded values would lighten every texture with contrast in it. Reduced after decoding rather than by asking Hio for a smaller read, since a plugin may decline a resize and report it only by the size of the buffer it returns. Changing the setting re-decodes each image into the slot it already holds, because a generated material has the slot index compiled into it |
| 2026-09-16 | Renders that estimate the same image differently are compared region by region against their own noise | Two unbiased estimators of one image agree in expectation and have independent noise, so the gallery's per-pixel gate fails them or has to be loosened until it means nothing. `hdClaudeImageDiff --expectation` compares 16-pixel block means by their difference over the standard error the blocks' own spread gives, which includes real structure and so can only make a difference harder to call; each such comparison is paired with a stage that must fail it |
| 2026-09-17 | A dielectric's reflect/transmit split is taken at the microfacet in both modes, and the layer's macro factor is divided back out | MaterialX's layering convention is that a base knows nothing of the top's Fresnel: `layer` supplies it as `top.throughput`, the top's directional albedo at the *macro* normal. For a coat over a substrate that is the right statistic; for the two halves of one interface it is not, because what crosses a rough surface is decided per microfacet. Inside glass most microfacets a steep ray meets are past the critical angle, where nothing crosses at all, while the macro average stays well below one and hands the base light that cannot physically pass. So the transmission lobe now carries `1 - F(V.H)` in both modes and divides by `1 - A` to cancel what the layer will apply, which makes the layered form agree with RT exactly where they must: a smooth sphere reads 1.0047 against RT's 1.0046, where before it read 1.0021 |
| 2026-09-17 | A transmission-only lobe offers no direction when its refraction fails | Its refraction failing *is* total internal reflection, and the reflection lobe layered above it already reflects every microfacet, the ones past the critical angle included. Falling back to a reflection there made the layer report a density that did not describe the sampling -- `mx_layer_bsdf` mixes the base's density, and a base asked about a reflection answers zero, so each such sample was weighed by the top's density alone though both halves had produced it. The estimator then divided by less than it should, once per crossing. Returning no direction loses no energy, because the transmission response is zero in exactly those configurations; it costs variance. A rough sphere of `layer(R, T)` at alpha 0.3 read 1.1747 at the centre and 1.9173 to 2.4257 off it, and reads 0.8654 and 0.8234 after -- which is RT's 0.8840 and 0.8538, the deficit of question 10, with the gain that had been hiding it gone |
| 2026-09-14 | The furnace's reference is the response integrated by quadrature, not a second implementation | Item 2 compares the importance-sampled albedo with the same generated response integrated over the sphere. A host transcription of every closure could disagree with MaterialX for its own reasons and would then be a rival definition rather than a check; the quadrature is instead checked against closed forms where they exist. It catches what chi-squared cannot: a lobe that responds where the renderer discards has a sampler and density in perfect agreement and loses the light anyway |
| 2026-09-14 | DLSS SDK moves from 310.3.0 to 310.9.1, and the pinned tag is the one that gets built | Every NGX call hdClaude makes is unchanged, and on the RTX 5060 Ti the runtime reports `NVIDIA DLSS 310.9.1.0` through all seven suites. Preset F -- `stable` -- is now marked deprecated but still present, so it is honoured and reported once rather than removed or remapped. The upgrade found why the pin had never been able to move: `setup_usd_env.bat` pointed `HDCLAUDE_DLSS_SDK` at FetchContent's own checkout, which made CMake treat it as a hand-supplied SDK and stop updating it, and the tag was a cache default a build tree kept anyway. The first rebuild after editing the tag printed the new version and linked the old SDK. The script no longer sets it, the override is cleared when the variable is unset, and the tag is a plain variable |
| 2026-09-05 | The pbrlib override set is all-or-nothing | MaterialX resolves `#include` relative to the including file, so mixing one upstream closure with one hdClaude closure emits `struct ClosureData` twice. The set is exactly the 22 pbrlib files that include `mx_closure_type.glsl`; no stdlib file does |
| 2026-09-21 | Which frame a displacement's offset is in is read from the constructor's declared input type, and a terminal that is not a constructor is reported | MaterialX declares two constructors for one `displacementshader` struct -- `ND_displacement_float`, "scalar displacement amount along the surface normal direction", and `ND_displacement_vector3`, "vector displacement in (dPdu, dPdv, N) tangent/normal space" -- and they share a category, share an output type, and generate the same two lines, so nothing in the program says which was compiled. The declared type of their `displacement` input is the only thing that tells them apart, and it can only be read before generation. A terminal that is neither, such as `mix` of a scalar and a vector displacement, is in no frame the specification names, so it is reported and read along the normal rather than settled by a guess; a `dot` is followed instead, because MaterialX defines it as a no-op routing point |
| 2026-09-21 | The frame a displacement is evaluated in is accumulated per vertex, and its derivatives reach the kernel unnormalised | Displacement moves a vertex, and a vertex belongs to every triangle around it. A frame taken from one triangle, as `shade.comp.glsl` takes it at a hit, would give the same vertex a different answer per incident triangle and open the mesh along every edge, so each triangle's contribution is summed at its corners weighted by twice its area -- the length of a cross product the normal accumulation already computes, which stops a sliver from pulling a vertex it barely touches. What is *not* done at the vertex is the rest of the frame: orthogonalising against the normal, normalising, and reading the handedness off dP/dv are left to the kernel, so a material being displaced and the same material being shaded build their tangent from the same arithmetic rather than from two implementations that can drift. Where a vertex has more than one authored answer the rule is to keep an authored one: a UV seam keeps the first coordinate in index order, because the two sides of a seam sit at opposite ends of the map and their mean is a coordinate that appears nowhere on the surface |
| 2026-09-21 | A displacement is evaluated in object space, once per prototype | A prototype is object-space geometry shared by every placement of it, which is what lets an instanced asset fit in memory at all. A displacement evaluated in world space would be a different mesh per instance and the sharing would be gone, so the kernel hands the generated program object space as both its world and its object frame and a graph that asks for world space reads object space. The displaced positions then come back to the host, which is the first answer to the residency question below: a displaced prototype is an ordinary prototype, so its fingerprint, its BLAS reuse and its refit all keep working unchanged, and the round trip is paid at scene publication rather than in a frame |
| 2026-09-21 | A displaced surface is shaded with the normals of the shape it now has | The normal array a mesh arrives with describes where the surface used to be. Shading a displaced height field with it lights every peak as though it were still the flat plane it was cut from, which is a picture of a surface that is not there, so the normals are recomputed from the moved positions by the accumulation the frames already use. A face-varying array cannot survive that -- its two normals at a crease belong to the undisplaced faces, and the displaced vertex has one position and one neighbourhood -- so that case is reported rather than dropped in silence. On the height map the recomputation is worth rms 4.1e-4, which is small because the terrain is mostly flat: forcing every normal to (1, 0, 0) moves 8.6% of the frame against the 0.004% recomputing moves, so the normals are certainly reaching the closure |
| 2026-09-21 | The view a refinement level is derived from is sampled, not followed | Published geometry is what acceleration structures are built over and what the accumulated film depends on, so a level that changed with the camera would rebuild both on every viewport nudge and throw away the prototype reuse the fingerprints exist to provide. It would also make a render depend on the path the camera took to get somewhere rather than on where it is. So the camera is sampled when there is nothing to derive from, when a refinement setting changes, and when `Retessellate` is bumped; following it continuously is a setting of its own, off by default. `ShouldSampleView` is the whole rule in one function so that it can be asserted without a camera or a stage |
| 2026-09-21 | Geometry outside the frustum is refined to a floor, never culled | A path tracer sees what the camera does not: a reflection, a refraction, a shadow cast from off-screen, and every indirect bounce. Dropping off-screen meshes to their control cage would be a defect visible only in the scenes that have mirrors, which in this gallery is most of them, and invisible in the one being looked at while it is being tuned. The floor is a setting and it is cheap: on Pixar's Kitchen Set, keeping off-screen geometry at level 1 rather than 0 costs 4% of the triangles |
| 2026-09-21 | What limits refinement is a face budget, not a cap on the level | The cap was six, and a cap on the level is the wrong instrument: it gives every mesh the same answer whether or not it needed one, so a mesh that could have used more could not have it and a mesh that needed none was refined anyway. A budget on the *faces* is about the machine rather than about a preference, it can say which mesh it applied to and what that mesh wanted, and it applies whether or not the level is adaptive. The level's own bound is now ten, and exists only so that a typo cannot ask for 4^30 faces before the budget gets to refuse |
| 2026-09-21 | hdClaude asks for a resync through a scene index of its own, because the change tracker refuses | `HdChangeTracker::MarkRprimDirty` raises "Calling method on HdChangeTracker that requires emulation" when the render index is driven by scene indices rather than by a scene delegate, and that is the pipeline `usdrecord` and `usdview` have used since OpenUSD turned scene indices on by default. The resync hdClaude already had for the curve and subdivision settings had therefore never fired in either of them -- silently, because a resync that does not happen looks exactly like a setting nobody changed, and the one harness where it did work is the legacy path the tests use. A pass-through filtering scene index that changes nothing about the scene gives hdClaude somewhere to send the notice from; the change tracker stays as the fallback for a host still driving this through a delegate |
| 2026-09-21 | A material that samples an image on a mesh with no texture coordinates is reported, not given coordinates | Every sample then reads the same texel, so a texture map is a flat colour and a *displacement* map is a uniform change of size -- which is indistinguishable, in the image, from displacement not working at all. A `UsdGeomSphere` is the case that raised it and the coordinates are genuinely absent rather than dropped on the way in: OpenUSD's `_ComputePrimvarsDataSource` gives a generated sphere `points` and nothing else, and `GeomUtilSphereMeshGenerator` offers points and normals, so the mesh reaches every Hydra renderer this way. Manufacturing a spherical projection here would be hdClaude answering a question the scene did not ask, and would make the renderer's output stop being a statement about the asset. The report names the mesh, how many images the material samples, and whether one of them is the displacement. It lives in the scene store's snapshot because that is the only place a mesh and its material meet: a mesh does not know what its material samples and a material does not know what it is bound to. Alongside it, the renderer now says how far a displacement actually moved a prototype, low to high, because "it did not happen" and "it happened and was too small to see" are the same picture |
| 2026-09-21 | `hdclaude_set_surface_hit` is emitted for every material, empty when the material reads no geometry | It was emitted only when MaterialX's vertex-data block was non-empty, and that was accidentally always true: `PathTracerSurfaceNode` registers the position and the normal, so every *surface* graph reads geometry whatever the document says. A displacement graph has no surface node, and a displacement of a constant asks the mesh nothing -- so the first such material found that the kernel's unconditional call had no function to bind to. The signature is the ABI and only the body depends on the document, which is what the setter existed to arrange in the first place |

## Open questions

Tracked here rather than decided prematurely.

1. **Sort granularity.** Counting sort per bounce over all active paths, or a
   partial sort that only separates the few most expensive materials? Decide
   with a measurement in phase 5, not before.
2. **Shadow-ray batching depth.** One shadow queue flushed per bounce, or a
   deeper queue flushed per frame? Interacts with memory footprint at 4K.
3. **Displacement residency.** GPU displacement (phase 16) multiplies vertex
   memory by the refinement level. Whether to cache displaced positions or
   re-evaluate per BLAS build is a memory/time trade to measure.
4. **Resolved 2026-09-15: where two thirds of an emissive scene's light goes
   under DLSS.** collectiveproject001 frame 1080, lit only by the character's
   emissive eye, keeps 32.5% of the reference's mean through DLAA at 128 samples
   a frame, where the gold shader ball keeps 99.8%.

   *Answer:* both DLSS models attenuate very high-contrast features only a few
   pixels across, and hdClaude's inputs are correct. `hdClaudeImageDiff
   --energy` bands a reference's pixels by luminance and reports what a
   candidate kept in each, and it put 92% of the frame's light in 227 pixels:
   the eye itself, thin bright rings two or three pixels thick at 256 px. Every
   measurement below is those pixels, at 256 px unless stated.

   | | Emitter light kept |
   | --- | --- |
   | the frame handed to DLSS, 128 samples, unreconstructed | 94.0% (98.1% of the frame) |
   | Super Resolution, 8 frames of 128 | 32.2% |
   | Ray Reconstruction, 8 frames of 128 | 82.7% |
   | Super Resolution / Ray Reconstruction, 64 frames of 128 | 33.1% / 81.2% |
   | emission scaled by 0.01 | 32.2% / 87.8% |
   | clipping range (0.1, 10000) changed to (100, 2000) | 32.2% / 82.7% |
   | eye's base 0, and base 1 white (Ray Reconstruction) | 83.3%, 82.7% |
   | **1024 px** | **77.3% / 92.9%** |

   So it is not the estimator (the input keeps the light), not the length of
   the history, not the magnitude of the highlight, not the depth guide's
   distribution on a stage in centimetres, not the albedo guide, and not the
   analytic-light guide defect once suspected here (with light geometry off,
   the default, the camera cannot see a light). It moves with the feature's size
   in pixels and nothing else tested. That is a property of NVIDIA's models, not
   something hdClaude can supply a better input for; Ray Reconstruction, the
   model built for path-traced input, loses a sixth of it at 256 px and a
   fourteenth at 1024.

5. **Resolved 2026-09-14: a crescent at every joint of an implicit curve.**
   *Cause:* `extend` generated a curve hit after checking only `tMin`, never
   that it was nearer than the hit already committed. At a joint, a ray that
   has found the surface also meets the neighbouring segment's end sphere
   behind it, inside the solid, and the driver (RTX 5060 Ti, 616.92) committed
   that farther hit whenever its box was visited later. Vulkan's ray closest
   hit determination says a confirmed hit with t > t_max is dropped, so this
   is the driver not doing what the specification says; comparing against the
   committed t in the kernel is correct under either behaviour. With the
   comparison, the four-segment chain and the single capsule agree at every one
   of 36,864 depths, and the test now asserts exactly zero; reverting it on the
   same build brings back exactly 656. The shadow kernel had the same gap at
   the far end -- a hit beyond `maxDistance` was generated -- and a comb of
   curves lying wholly beyond a light took 9% of that light off the surface
   below it (ratio 0.9105, now 1.0000), which is asserted too. On ALab's stoat
   at frame 1004, the knitted jumper's yarn rendered as strings of beads, one
   bulge a joint, and renders as continuous strands after.

   What follows is the investigation as it stood, kept because the exclusions
   are still true. Reported from usdview as
   circular patterns scattered through ALab's fur, and reproduced: a straight,
   constant-radius chain of segments renders with a dark crescent at each joint,
   where the same capsule described as *one* segment renders perfectly smooth
   (rms 0.0089 between the two descriptions, 0.56% of pixels, all of it at the
   seams). `HDCLAUDE_CURVE_GEOMETRY` defaulted to `swept` while this was open, and
   defaults to `implicit` since 2026-09-14 at Paolo's request.

   What has been excluded, each with the install verified afterwards -- several
   early attempts were run against a stale installed shader and are void:

   * **Not the caps.** Removing the end spheres from the intersector changes
     nothing.
   * **Not the shading normal.** Forcing it to a constant leaves the crescent
     exactly where it was.
   * **Not shadow rays from curves.** Making curves cast no shadow at all is
     bit-identical (rms 0).
   * **Not the light's shadows.** Disabling them on the light leaves it.
   * **Not the geometric normal's branch.** Forcing the cone normal everywhere
     changes the image but not the crescent -- though this *did* find a real
     latent fault: `hdclaude_segment_normal` extrapolates its cap branch for a
     point beyond the segment's end and returns a normal up to 31 degrees off
     radial. Nothing is known to ask it that today.
   * **Not the intersector's arithmetic.** A host transcription of
     `hdclaude_intersect_segment` agrees to 1e-6 between a four-segment chain
     and the single capsule it describes, over rays from the real camera
     straddling a joint.

   What is known: it needs the rect light (uniform illumination renders the same
   geometry perfectly smooth), it survives that light's shadows being switched
   off, and it survives a constant shading normal -- which together say the
   variation is not in the normal and not in occlusion. The next thing to look
   at is `point.position` itself, and whether a generated intersection's
   distance means the same thing in the world ray's parameterisation as in the
   object ray's when the two are handed to different kernels.


   **Narrowed since.** It is geometry, not shading: a chain of four segments
   describing one capsule renders 656 of 36,864 *depths* different from the
   single segment describing the same solid, worst 0.00163, and depth carries no
   lighting. `tests/render_tests.cpp` measures exactly that and prints it.

   Three further things are excluded. It is not the algorithm -- a host
   transcription in double precision agrees exactly with the single capsule on
   the very rays that differ on the device. It is not conditioning -- rewriting
   the quadratic with cross products, which removes the cancellation in
   `|ba|^2|oa|^2 - (ba.oa)^2`, and rewriting the root in the numerically stable
   form both leave the result bit-identical. Taking the far root when the near
   one falls outside the segment makes it *worse* (719, 0.00314), because that
   root is where the ray leaves the solid.

   What did move it: growing every AABB threefold takes it from 656 to 599. So
   part of this is boxes rejecting a grazing hit that the surface inside them
   would have answered, after which an interior sphere -- which a single capsule
   does not have -- supplies a farther hit instead of none. That is a lead
   rather than the answer, since padding removed only a tenth of it.

7. **Withdrawn 2026-09-17: "a thin-walled transmissive sheet passes nothing on
   a closed shape."** The entry recorded **0.0769** -- exactly the sheet's own
   reflectance -- for a thin-walled `open_pbr_surface` sphere of pure
   transmission, at three bounces and at thirty-two alike, and called it a
   regression in the thin-walled work of 2026-09-15.

   It does not reproduce. Nothing under `mtlx/`, `shaders/` or
   `tests/render_tests.cpp` changed between the commit that recorded it and the
   one that withdraws it, and the same printed line now reads 0.9246 at three
   bounces and 0.9951 at eight, sixteen and thirty-two: a thin-walled sphere
   converges, and the 0.5% it is short of one is the same deficit the solid
   sphere has. Two runs of the suite agree to the last digit, so it is not
   nondeterminism either. The likeliest explanation is that the number was
   measured while a diagnostic edit was still in the working tree -- that
   session had been switching the thin-walled path on and off by hand for most
   of a day -- and the lesson is in
   [debugging-a-render.md](debugging-a-render.md): a measurement is only
   evidence if the tree it was taken from is the tree that gets committed.

   The hunt was not wasted. The entry's other half -- "against 0.95 to 1.03 for
   the identical material rendered solid" -- was a two-point sample of a defect
   that a ladder exposes at once, and it is question 10.

8. **Subsurface loses a tenth of its light in a furnace.** The same sphere with
   `subsurface_weight` 0.2 and a radius of 0.1 reads 0.83 solid and 0.90
   thin-walled, flat across bounce counts. It is not a regression -- it predates
   the thin-walled work -- and the closure's own validation suite passes, so
   what the furnace measures is something the per-closure tests do not: the
   boundary and the walk together, over a shape a path re-enters. Printed and
   not asserted.

9. **Answered 2026-09-17: implicit surfaces are not rendered.** hdClaude now
   registers `HdsiImplicitSurfaceSceneIndex` and asks it for meshes, so the
   stage below renders five shapes where it rendered an empty frame:
   `tests/usd/implicit_surfaces.usda` arrives as 5 prototypes, 5 instances and
   7,312 triangles, and each shape reads 0.17 to 0.18 against a background of
   1.00, at the frame position its own camera arithmetic predicts. The test is
   gated as `usd_implicit_surfaces`, and it was run with the plugin removed
   from the *installed* plugInfo to check that it fails: every shape then reads
   the background and all five fail, which is what says the test measures the
   conversion rather than the dome.

   The original finding follows.

   A stage whose only geometry is a
   `UsdGeomSphere` arrives as "0 prototypes, 0 instances": spheres, cubes,
   cylinders, cones and capsules reach a delegate as their own prim types, and
   OpenUSD ships `HdsiImplicitSurfaceSceneIndex` to turn them into meshes (or
   into the axis-aligned forms a renderer can intersect directly) without
   inserting it for anyone. It is the same opt-in that the NURBS approximation
   and light linking needed, and Karma renders these where hdClaude renders
   nothing. Found 2026-09-16 while building a cross-renderer test.

10. **Mostly answered 2026-09-17: a rough dielectric loses light on a closed
    shape, and the rougher it is the more.** The compensation described below
    is now in `mx_dielectric_bsdf`, fitted to the measured grid, and the spheres
    read 0.9953, 0.9859 and 0.9425 in RT mode at alpha 0.1, 0.3 and 0.6 against
    0.9826, 0.8761 and 0.6321 without it -- and 0.9951, 0.9657 and 0.9117
    layered, against 0.9825, 0.8568 and 0.6021. Off-centre, where the interior
    angles are steepest, the layered sphere reads 0.8290 at alpha 0.6 against
    0.5337. The smooth spheres do not move by a digit, which the form
    guarantees rather than achieves: every term of the fit carries a factor of
    alpha.

    **What is left of it** is the fit's own residual, which compounds over the
    six or so crossings a sphere makes: 1% at alpha 0.1, up to 7% at 0.3 and
    17% at 0.6 in the worst corner. The suite asserts one to those bounds, so a
    regression that lost the compensation would read 0.63 and fail loudly, and
    they tighten when the residual does. Closing the rest means a better fit or
    the table it approximates -- the grid generator is committed and the
    measurements are the constraint.

    The original finding and its evidence follow.

    A sphere of `dielectric_bsdf` in RT mode at alpha 0.3 reads
    0.8840 at the centre of the disc and 0.8538 off it in a white furnace at
    thirty-two bounces; `layer(R, T)` of the same interface -- the structure
    `open_pbr_surface` builds -- reads 0.8654 and 0.8234. The same two smooth
    read 1.0046 and 0.9902. An `open_pbr_surface` sphere of pure transmission at
    the specification's default `specular_roughness` of 0.3, which its own
    mapping squares to an alpha of 0.09, reads 0.9858 at thirty-two bounces.
    A lossless interface is lossless at every roughness, so all of these must
    read one.

    What they measure is the energy single-scattering GGX loses to masking.
    MaterialX compensates its *reflection* lobe -- `mx_ggx_energy_compensation`,
    built from the reflection-only directional albedo -- and has no term at all
    for the transmission lobe, so the light shadowed out of a crossing is simply
    gone, and a path that crosses a sphere a dozen times loses it a dozen times.
    OpenPBR specifies a specular BSDF that conserves energy, so this is a
    shortfall against the specification rather than a reading of it.

    The fix is a multiple-scattering compensation for the interface as a whole
    rather than for one of its lobes -- Turquin (2019) tabulates the dielectric
    directional albedo over cos(theta), alpha and eta, in both crossing
    directions -- which is phase-4 work: a table, its generator, and its own
    chi-squared and furnace gates. Until then the measurements are printed by
    `tests/render_tests.cpp` and not asserted, over a ladder in alpha, because a
    reading at roughness zero cannot see this at all.

    **A closed form derived from the reflection lobe does not work, and the
    measurement says why.** Requiring the two halves of a lossless interface to
    sum to one gives the transmission lobe a factor with no free parameter in
    it, if one assumes the transmission's single-scattering albedo is
    `(1 - F) Ess` for the reflection lobe's own `Ess`:

        k = [1 - F Ess - F^2 (1 - Ess)] / [(1 - F) Ess] = (1 + F) / Ess - F

    It has both right limits -- exactly one when `Ess` is one, so no smooth
    furnace moves, and `1 / Ess` when nothing reflects -- and it overshoots
    anyway. Measured on the spheres: 0.993 and 0.991 at alpha 0.1, but 1.15 and
    1.12 at alpha 0.3 where the uncompensated renderer reads 0.88 and 0.87, and
    1.82 and 1.73 at alpha 0.6. Roughly three times too much light per crossing.

    So the assumption is what fails: the transmission lobe loses far less to
    masking than the reflection lobe does at the same roughness, which is what a
    refracted direction bending toward the normal would do, and its albedo
    depends on the index through the refraction Jacobian besides. It cannot be
    had from `mx_ggx_dir_albedo`, which is a reflection-only fit. The next
    attempt needs the transmission lobe's *own* directional albedo, measured
    rather than derived, which is the table -- and this is the constraint it
    has to satisfy. The ladder without compensation, at n = 1.5, thirty-two
    bounces, `RT` and `layer(R, T)` at the centre of the disc and off it:

        alpha 0.1   0.9826  0.9825   |   0.9777  0.9690
        alpha 0.3   0.8761  0.8568   |   0.8546  0.8146
        alpha 0.6   0.6321  0.6021   |   0.6114  0.5337

    All twelve must read one, and the smooth readings beside them -- 1.0046 and
    1.0047 -- must not move.

    **The interface's own albedo is now measured, and it settles the design.**
    `HDCLAUDE_ALBEDO_GRID=1` on the closure validation suite prints it: the
    quadrature integrates the response over the whole sphere, evaluating each
    direction as the shade kernel would, so an `RT` lobe's integrated albedo is
    the whole interface and an `R` lobe's is its reflection half. At n = 1.5,
    entering:

        alpha   theta 0.2   theta 0.6   theta 1.0
        0.1     0.9988      0.9980      0.9939
        0.3     0.9881      0.9807      0.9552
        0.6     0.9545      0.9312      0.8732

    That is the per-crossing loss, and it predicts the spheres: 0.98^6.5 is
    0.877 against a measured 0.8761 at alpha 0.3, and 0.93^6.5 is 0.63 against
    0.6321 at alpha 0.6. So the deficit is exactly "one crossing's albedo, as
    many times as the path crosses", and the fix is to divide by it.

    It cannot be had from the reflection lobe's fit. Against `Ess` from
    `mx_ggx_dir_albedo(cos theta, alpha, 1, 1)`, the interface's deficit is
    between **7% and 38%** of the mirror's, varying by a factor of five over
    the same grid and strongly with angle -- 0.08 at theta 0.2 against 0.34 at
    theta 1.0, at alpha 0.6 -- so it is not `(1 - Ess)` times any function of
    the index alone. A dielectric loses far less than a mirror of the same
    roughness because most of its light transmits, and a refracted direction is
    much less exposed to masking. Hence the table, over cos(theta), alpha and
    eta, with the grid generator above as its source.

    Worth recording beside it: the *mirror* is already lossless. The `Ess`
    column of that grid, measured on `generalized_schlick` with both colours
    white, reads between 0.9873 and 1.0130 at every roughness, because
    MaterialX's compensation is applied to it. The defect is not that the
    renderer cannot conserve energy at a rough interface; it is that only one of
    the two lobes was ever given the term that does it.

    **The two encodings also part company at alpha 0.6**, by 12.7% off-centre,
    where they agree to a fifth of a per cent when smooth and to 4.7% at alpha
    0.3. That is a second thing to explain, and it is not the division guard in
    `mx_dielectric_bsdf`: lowering that floor from 0.02 to 1e-4 changed the
    reading by nothing at all, to the last digit. The layered form is the one
    that loses more, and what it has that `RT` does not is `mx_layer_bsdf`
    selecting on a directional albedo that comes from a *fit* -- which at alpha
    0.6 is the regime where a fit of that family is worst. Suspected, not
    shown. The suite asserts the agreement up to alpha 0.3 and records alpha
    0.6 rather than asserting a tolerance wide enough to cover it.

    Recorded 2026-09-17, after two density defects that had been hiding it were
    fixed (decision log). Before them the same rough `layer(R, T)` sphere read
    1.1747 at the centre and 1.9173 to 2.4257 off it -- a gain, and the gain and
    the deficit were partly cancelling.

6. **Volume rendering.** MaterialX VDFs are declared and generated; hdClaude
   currently plans homogeneous interior media only. Heterogeneous volumes
   (`UsdVol`) are not scheduled.
